# Candidate North Star Output Report v0.5 Codex

## Status

Output report for:

```text
Candidate North Star v0.5 Codex.md
```

This is not canon.
It does not promote the candidate.
It does not replace `NORTH-STAR.md`.

## What Changed From v0.4

v0.5 Codex is a compact patch release over v0.4.

Main changes:

- renamed the draft as `v0.5 Codex`;
- added the explicit default-equality guardrail:

```text
same payload
same current value
```

are not default visible-state equality;

- changed formal residue wording from `immediate prior-art absorption` to
  `tested prior-art absorption`;
- added a per-witness falsifiability line: a witness fails if a mature
  absorber supplies a natural sufficient enrichment or quotient;
- added approximate-equivalence to the reviewer checklist:

```text
epsilon / probabilistic / top-k / recall@k / latency-recall / workload
```

- added a domain-calibration gate sentence making clear that domain examples
  are pressure tests, not novelty evidence;
- replaced the flat appendix list with a burden table mapping companion
  reports to the work they carry.

## What Did Not Change

The central posture remains:

```text
Capability Projection is projection sufficiency for typed capability objects.
```

The locked constraints remain:

- non-factorization is old;
- global absorption comes first;
- typed `Cap` and declared equivalence come before examples;
- finite pair tests are necessary but weak;
- canonical residue remains hard;
- physics enters only as:

```text
known physics -> induced Cap -> projection sufficiency audit
```

not:

```text
Cap -> known physics
```

## Bottom Line

v0.5 Codex keeps v0.4's compact audit-specification shape and applies the
pre-v0.5 readiness patches without expanding the main note back into an
encyclopedic synthesis.
