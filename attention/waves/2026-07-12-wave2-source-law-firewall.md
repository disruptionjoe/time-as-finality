# Wave 2 - Source Law Firewall

Date: 2026-07-12

## Purpose

Start the post-Wave-1 successor work while applying the two-track exploit
discipline:

```text
Track 1 = North Star pursuit.
Track 2 = subordinate branch that reports up.
```

Wave 1 left the top state as:

```text
TAF9 > TAF10 > TAF3 > TAF8 > TAF4 > TAF6 > TAF5 > TAF7
```

TAF9 is the controlling objective. TAF10 can run only as a branch that may feed
TAF9 or provide a later TAF3 target. TAF3 remains dependent.

## Wave Boundary

This wave has two parallel swings:

1. Track 1, TAF9, T534: search for a new finality-domain source law with
   independent comparability density, or produce an honest pause/demotion
   router for the S1 finite-generator route.
2. Track 2, TAF10, T535: search for a real C(R) surplus packet replacing the
   T533 synthetic target, explicitly subordinate to Track 1.

TAF3 does not execute in this wave unless T534 or T535 supplies a concrete law
or noncompletion target.

## Track 1 - T534

Working T-number: T534.

Owned target:

```text
tests/T534-*.md
models/t534_*.py
tests/test_t534_*.py
results/T534-*-v0.1-results.md
```

Question:

```text
After T532 killed the first receipt-product candidates, is there a credible new
finality-domain law with independent comparability density?
```

The candidate menu must exclude:

- the already-negative two-channel receipt product;
- the density-mismatched three-channel receipt product;
- Lorentzian u/v reference import;
- T528/T530 screen conditioning;
- post-hoc repaired-band fitting.

Allowed outcomes:

- CLEARED: a new law clears as a real candidate.
- NARROWED: a candidate survives only with stated missing burdens.
- FALSIFIED: a candidate fails by computation or absorber.
- PAUSE: no credible new source law exists in hand; pause the S1 finite-generator
  route until one appears.
- REVIEW_ONLY: useful screen or calibration only.

Difficulty alone is not falsification. A PAUSE result is not a North Star
demotion unless it states an actual falsifier.

## Track 2 - T535

Working T-number: T535.

Owned target:

```text
tests/T535-*.md
models/t535_*.py
tests/test_t535_*.py
results/T535-*-v0.1-results.md
```

Question:

```text
Can an existing repo packet replace T533's synthetic future C(R) target with a
real packet that has exact full-stack profile matches plus an independent
non-task-success noncompletion witness?
```

Candidate sources should come from existing repo material, such as:

- the T407/T398/T493/T494/T500/T529 C(R) family;
- the T411/T412 physical-boundary lineage;
- the T514-T520 quantum access or monogamy lane;
- the T521 detector provenance lane.

Allowed outcomes are the same as T534. A Track-2 CLEARED result does not change
the repo posture by itself. It reports up as a TAF3 target or a Track-1 stress
test.

## TAF3 Firewall

TAF3 may execute only if:

1. T534 supplies a real source-law candidate, or
2. T535 supplies a real noncompletion target, or
3. both fail in a way that names a sharper source-law falsifier or branch
   demotion target.

Without one of those, TAF3 remains blocked.

## Generative Re-Rank Trigger

After T534 and T535 land:

1. Run the council inline in one worker.
2. Reflect on what the wave taught.
3. Draft new hypotheses before voting.
4. Vote strict ballots.
5. Run `python attention/taf_priority_condorcet.py`.
6. Update `attention/research-steering-card.md` and this wave packet.

Do not let a finishable branch outrank the North Star merely because it is
finishable.

## Wave Outcome

Status: completed.

### T534 Outcome

Artifact:

```text
tests/T534-new-finality-domain-law-pause-router.md
models/t534_new_finality_domain_law_pause_router.py
tests/test_t534_new_finality_domain_law_pause_router.py
results/T534-new-finality-domain-law-pause-router-v0.1-results.md
```

Verdict:

```text
pause_s1_finite_generator_route_no_cleared_new_source_law
```

Wave lesson:

- The candidate menu excluded the T532-negative and forbidden routes.
- `record_window_separation_order` is genuinely finality-domain native.
- Exact endpoint enumeration gives comparability density 2/6 = 1/3, not the
  target 1/2.
- No new source law clears.
- Difficulty alone is not falsification, but the S1 finite-generator route
  should pause until a new source law exists.

### T535 Outcome

Artifact:

```text
tests/T535-real-taf10-packet-screen.md
models/t535_real_taf10_packet_screen.py
tests/test_t535_real_taf10_packet_screen.py
results/T535-real-taf10-packet-screen-v0.1-results.md
```

Verdict:

```text
NO_REAL_TAF10_PACKET_IN_HAND_PAUSE
```

Wave lesson:

- Five existing-source candidate families were evaluated.
- Current C(R) remains review-only over weaker statistics.
- T411/T412 and T520 are absorbed.
- The quantum access/monogamy lane narrows a different question.
- T521 is a manifest template, not a data-bearing packet.
- The missing field is a real packet with exact competency/resource/permission/
  provenance profile matches plus an independent non-task-success
  noncompletion witness.

### TAF3 Status

TAF3 remains blocked. T534 supplied no source law and T535 supplied no real
noncompletion target.

### Two-Track Firewall Check

The branch did not outrank the North Star. T535 is useful negative branch
evidence, but it does not replace the Track-1 source-law question.

### Generative Re-Rank

The council reflection generated two successor hypotheses:

- TAF11 - North Star source-law reassessment beyond the finite-generator route.
- TAF12 - data-bearing C(R) packet acquisition for TAF10.

The Condorcet result after Wave 2 is:

```text
TAF11 > TAF8 > TAF3 > TAF12 > TAF4 > TAF6 > TAF5 > TAF7
```

The new Condorcet winner is TAF11.

## Discipline

- Honesty above all. NARROWED and FALSIFIED are SUCCESSES, not failures.
- Research advancement of THIS repo only.
- One durable, reproducible artifact per swing, committed and pushed.
- Personas always inline, never per-agent.
- No em-dashes in prose.
