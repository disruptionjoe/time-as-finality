# Wave 3: Descent-Obstruction Resolution Stress Test

Date: 2026-07-12

## Method Imported

AI epistemology supplied the orchestration discipline: bounded fronts, explicit
closeout states, watch conditions, and no claim promotion without a durable
artifact.

GU formalization supplied the active wave pattern: a dependency-bounded swing,
a deterministic computation that exits 0, a short writeup with COMPUTED versus
ARGUED labels, inline council reflection, and Condorcet re-ranking after the
batch.

## Wave Boundary

Wave 3 attacked the dependency opened by T537:

`T537 selected descent_obstruction_resolution_family -> T538 tests whether the
first computable packet is law-like or programmable.`

This wave was serialized because TAF4, TAF3, and Track-2 C(R) work all depend
on whether TAF11 can produce a source law or a real generator obligation.

## Fronts

| front | state | durable artifact | result |
| --- | --- | --- | --- |
| T538 descent-obstruction resolution source-law packet | complete, NARROWED | `models/t538_descent_obstruction_resolution_source_law_packet.py`, `tests/test_t538_descent_obstruction_resolution_source_law_packet.py`, `results/T538-descent-obstruction-resolution-source-law-packet-v0.1-results.md` | The ordered-depth recipe is target-free and invariant under shallow relabeling controls, but free depths can program antichain, total-chain, diamond, and fork relations. The family needs a resolution-depth generator before it can count as a source law. |

## Inline Council Reflection

Orthodox professor: the current packet reduces to a parameterized partial-order
decoder unless the depths are generated by the cover and restriction data. The
missing theorem is not invariance under relabeling; it is nonprogrammability.

Heterodox-rigorous professor: the hidden assumption is that
`resolution_depth` is already source data. T538 exposes that assumption. If
depths are free, the family can smuggle any relation into the source layer.

Commercial scientist: the cheap decisive test worked. The next cheap test is a
generator gate: remove pairwise depth inputs and see whether the local sections
force depths.

Philosopher of science: the family is not killed, but it is exposed. It dies if
no nonprogrammatic generator can produce depths before relation choice.

Wild mathematically-serious frontier scientist: the upside object is a typed
depth generator, possibly from obstruction persistence across allowed
refinements. That is the only way this branch becomes more than a decoder.

Category, sheaf, and obstruction lens: descent data must determine the
obstruction persistence, not merely label it. The next packet should treat
depths as output, not input.

Causal-set and Lorentzian lens: T538 correctly avoided target coordinates.
TAF4 stays blocked because no source law exists yet.

Resource and C(R) lenses: Track 2 still does not replace Track 1. No real
TAF10 packet appeared during this wave.

## Synthesis

Coherence gap: the repo still lacks a finality-native generator that turns
local record-cover and restriction data into relation-producing quantities.

Local minimum detected: the source-law lane keeps building routers and
admission gates that name the next object, while the decisive generative step is
deferred. T538 breaks that pattern by showing that the named object is
underconstrained unless its depths are generated rather than supplied.

Highest-leverage next moves:

1. T539 resolution-depth generator packet. Derive ordered depths from local
   record-cover, restriction-map, and compatible-section data with no pairwise
   depth table.
2. If T539 fails, retire the descent-obstruction resolution family and re-rank
   TAF11 against TAF8 rather than building another router around the same
   variables.
3. Keep TAF4 blocked until TAF11 supplies a nonprogrammatic source law.

## Generative Re-Rank

New candidate drafted from the wave:

- `TAF11/T539`: resolution-depth generator packet. Force or kill the
  descent-obstruction family by treating depths as computed outputs.

Council vote keeps TAF11 first because the North Star is not falsified. TAF8
stays second because, if T539 fails, the next best high-value branch is the
cross-domain shadow-protection theorem target rather than another programmable
source-law decoder.

Run:

```bash
python attention/taf_priority_condorcet.py
```

Wave 3 changes the TAF11 reason but not the top-level order.
