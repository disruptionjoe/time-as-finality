# The Commit Module S6: Series Conclusion — CM = (I, G, D) at Its Earned Form

**Status:** swing 6 of 6 — the series conclusion; **series COMPLETE**;
review-only — no claim movement, no T-number, no bin regrade, no guardrail
edit, no posture change, and no tick-family credit implied or banked. The
enactment slate (§4) is **proposed, not enacted** — the orchestrator enacts
after the closers. Steward grading of the series is a separate open step.
Form: the house conclusion form
([adversarial-research-practice-2026-07-28.md](adversarial-research-practice-2026-07-28.md)
instrument 12) — what is forced, with witnesses and scope; what remains
open, with reopeners; the comparison at its earned form; under the two
legitimacy rules (interpretation-only is a legitimate terminal state; **a
precise failure is a successful outcome** — and this series contains one,
taken honestly, in §2's L4). None of the verdicts below is a truth verdict
about nature; each is the closure or pricing of a stated route.
**Date:** 2026-07-28
**The series record (all read in full for this note):**
[S1 — schema](commit-module-schema-2026-07-28.md) (the module statement,
K-CM, the dependency DAG, Debts L1–L4);
[S2 — issuance from redundancy](commit-module-s2-redundancy-issuance-2026-07-28.md)
+ [probe](../models/redundancy_issuance_probe.py) (PARTIAL — kill fired
scoped);
[S3 — capability-graded finality](commit-module-s3-capability-graded-finality-2026-07-28.md)
+ [probe](../models/capability_graded_finality_probe.py) (kill does not
fire);
[S4 — the decomposition debt](commit-module-s4-decomposition-debt-2026-07-28.md)
+ [probe](../models/decomposition_sweep_probe.py) (route A succeeds at
fixture level up to the T584 orbit);
[S5 — the Wigner-friend settlement](commit-module-s5-wigner-settlement-2026-07-28.md)
(SPLIT). All three probes were re-executed by this arm before writing:
26/26, 16/16, 27/27 gated checks match, exit 0 each — the siblings'
check-counts are confirmed by re-run, not carried on trust.
**Comparison surfaces:**
[rival-symmetry-swings-2026-07-28.md](rival-symmetry-swings-2026-07-28.md)
(the cost column; the earned-form blockquote);
[rival-map-2026-07-28.md](rival-map-2026-07-28.md) (row 6);
[goal2-charter-verdict-2026-07-28.md](goal2-charter-verdict-2026-07-28.md)
§3 (the rival clause this series completes); VERDICT.md (the mirror
sentence). Wording-freshness: the two pending P1 mailbox notes on the
private system-runtime surface were read (pointer only, per public-repo
hygiene); **no steward annotation changes the rival's earned wording**, so
the in-repo wording governs throughout.
**Tags:** `research_note` · `review_only` · `series_conclusion` ·
`swing_6_of_6`

---

## 1. The module's earned form (what is forced, with witnesses)

The series set out to make the rival's "thermodynamic commit module" — the
posited component that survived the R1–R3 swings only as a named black box
— a stated, derived-where-possible, priced-everywhere object. That is now
done, component by component:

**G — the commit grade: independently statable, delivered, and checked.**
Independently statable (S1 §2 — the cycle breaker: reversal cost is defined
with zero record, issuance, or finality vocabulary; T587's own *rejected*
classes have well-defined G-values, confirming G presupposes no I).
Delivered and checked (S3, probe 16/16): commit(A, r) is **T584-invariant**
(representation, gauge, and declared irrelevant coarse-graining preserve
FINAL, typed status, and the full margin vector; the inadmissible
task-merge is caught — the must-fail control fails closed), **monotone
under capability extension** (61/61 comparable pairs lawful; 148 genuine
finality flips, all in the extending-un-finalizes direction), and
**limit-recovering** (every finite-ω record un-finalizes at a finite sweep
scale; the declared divergent edge stays FINAL uniformly over every
declared bounded class; FINAL for all bounded classes ⟺ W_rev diverges,
with the constructive converse). The past of class A = {r : un-commit(r) ∉
Env(A)}, and horizon kind-finality is the W_rev → ∞ boundary of the *same*
grade — no second finality concept. Refinement from S4, tested: G is
**typing-dead and grouping-live** — one grade table across all 8192
retypings; values move only with the grouping half, where the fixture's own
checks admit exactly the T584 orbit {g1, g3, g4}. S1's "independent as a
definition, parametric in use" is confirmed as refined. One declared
reading carried, not smuggled: the *identification* (committedness :=
graded inaccessibility of reversal) remains a declared identification (S1
§1's G row), priced by L2 — the functional is derived; the reading of it as
finality is the module's claim.

**D — the decomposition: derived up to the T584 orbit at fixture level,
priced in declaration currency.** S4's route A (the fixture-expressible,
task-functional shadow of predictability-sieve selection: C2 completeness +
C3 grade-stability + checked causal alignment) selects, out of 8192
enumerated typings × 5 groupings, exactly the T586 declaration's
equivalence class: grouping half **derived up to the T584 orbit**; typing
half **derived on the interior** (consumed carriers forced in, non-carriers
forced out) and **conventional-but-constrained at the fixture boundary** (a
two-bit terminal-record horizon convention that moves issuance typing and
record count, never an order edge, never a grade). Nowhere on the fixture
class is D a bare primitive; the L3 kill's firing condition (both routes
circular) is not met at fixture level, and the declared-primitive fallback
never fires. The price is stated in **declaration currency** — the declared
task context the module already pays for, a shared grade-stability
ingredient, and **two bounded conventions** (the T584-orbit representative,
already contracted; the terminal-record horizon convention, **the only
genuinely new fixture-level cost** — S4's price-table row 5). The G↔D loop
is adjudicated benign (finite declared candidate family; selection is a
filter, not a fixed point); the vicious I↔D cycle is untouched by the
derivation (C1–C3 consume no issuance typing).

**I — the issuance rule: NOT independently derivable from redundancy;
derived-conditional-on-(G, D).** S2 executed repair route 2 (the SBS /
strong-quantum-Darwinism condition, N10's primary set) against its
registered kill and returned **PARTIAL — the kill fired scoped**, on the
protocol's exact wording. The two horns, both measured: at R = 1 (the only
reproducing configurations) the criterion is provably
carrier-nonemptiness — the issued-event set is extensionally the
producing-event set, i.e. **T587's own admissibility predicate restated in
carrier vocabulary** (degenerate; a restatement may not be counted as a
derivation, per K-CM's own discipline); at every non-degenerate
configuration the selection is **split-borne** — no non-vacuous R = 2
reproduction survives the single-block fragment partition (0/400 family,
0/60 copy-augmented), and the mechanism-honest provenance grouping breaks
reproduction at the copy step even at R = 1 (frozen fixture 4/6 pairs;
60/60 augmented). So I is **derived-conditional-on-(G, D)**: given the
declared D (and, for the (c)-clause, G's stability content — the I→G edge
exhibited, not hidden), the reproducing form exists and is exact across the
causally-aligned family (200/200 both regimes; class screen coextensive at
R = 1). The typing's supplied status stands; its physical content is now
stated, tested, and priced; **the primitive count transfers to D** — where
S4 then prices it: the condition I is conditional on is itself derived up
to a declared equivalence class from already-priced ingredients. What the
transfer lands on, after S4, is not a bare primitive but D's two bounded
conventions. Route 1 (threshold on G given D) remains untested; S2's own
closing observation is that route 2, pushed for content, degenerates into
route 1 (a priced copy is a capability-contract quantity) — the routes are
not independent on this fixture class.

### The terminal branch verdict

**S1's relabeling conviction does NOT fire.** K-CM's terminal branch — "if
S2 fails both repair routes AND S4's fallback declaration fails to make I
and G well-defined and fixture-reproducing, then … CM is convicted as a
relabeling" — is doubly unmet: S2 returned PARTIAL, not FAIL (a
D-conditional reproducing form exists), and S4's fallback was never needed
(route A landed). Stated positively, with the three witnesses:

1. **G is independent** — statable with zero finality vocabulary (S1 §2)
   and executed to L2's demanded shape (S3, kill not fired);
2. **D is derived up to a declared equivalence class at fixture level** —
   not a bare primitive anywhere on the fixture class (S4's route-A
   verdict, 27/27);
3. **I is conditional-derived** — its physical content stated and
   order-reproducing given (G, D), with the residual suppliedness priced as
   D's conventions (S2's PARTIAL + S4's price table, jointly).

A module with one independent component, one component derived up to a
priced equivalence class, and one component derived conditional on the
other two **is not a relabeling**: "issued record" has a statement whose
only non-derived content is priced declaration structure, not a
finality-shaped primitive. The repaired module is the DAG S1 drew —
standard thermodynamics → G; consumption structure + declared context +
grade stability → D (fixture level); (G, D) → I — with every arrow now
carrying an executed witness.

---

## 2. The debt ledger, closed out

Dispositions after S6, each with exact scope and reopeners. (The schema's
ledger table is headed "status after S1"; the slate note in §4 addresses
how these dispositions reach that surface.)

| debt | disposition after the series | witness |
|---|---|---|
| L1 (I supplied) | **DISCHARGED-CONDITIONAL** (fixture level) | S2 PARTIAL + S4 route A, jointly |
| L2 (G FAPP-grade) | **DISCHARGED** (fixture level) | S3, kill does not fire |
| L3 (D privileged) | **PARTIAL**, per S4's own offer | S4 route-A verdict + price table |
| L4 (Wigner exit) | **SPLIT**, per S5 | S5 §5 |

**L1 — discharged-conditional.** The discharge is joint: S2's scoped fire
establishes the D-conditional reproducing form and the coextension of
T587's typing with stable-consumable-carrier existence across the aligned
family; S4's D-derivation prices the condition itself, so
"derived-conditional-on-D" bottoms out in declaration currency rather than
in an unexplained privileged choice. Exact scope: causally-aligned regimes
only; the frozen T586 fixture, the 2×200 sweep family, and the 60-fixture
copy-augmented subfamily; reproduction-not-grounding (the source question
stays TI's). This is **not** a derivation of I from redundancy structure
doing independent selective work — that claim failed measurably, and the
failure is the finding. Reopeners: (a) the richer frozen fixture family
(multi-consumer, conditional-state, priced-copy — §7), which is also the
only venue where route 1 gets a genuine test; (b) **the DU-holdings gate
arm's report** — still absent from this repository at S6 write time
(searched: TaF explorations and run surfaces, DU explorations,
system-runtime mailboxes). Per the schema's boundary sentence 2 and S2's
own provenance, **S2 may not be treated as closed until that report is
consulted**; the dependency is carried open here, with S2's recorded
mitigations (mechanism-template-only SBS usage; a degenerate-or-fragile
verdict that treats no SBS/QD physics as landed support; both DU pointers
verified by direct reading at pin `b190306`); resolution: the gate ran
report-only — its GO/SEAM verdicts will be recorded in the series'
closeout mailbox note; cite forward to that note as the consultable
record (no in-repo report will appear); (c) route B's fixed point
(continuum framing), S2-coupled and unresolved.

**L2 — discharged.** The demanded functional exists and runs: invariance,
monotonicity, limit recovery, plus the unregistered-but-passed
order-compatibility check (un-commit closures nest; finality propagates
upstream; consumer proliferation only hardens ancestors). "FAPP" now has a
formal referent — final-at-grade for a declared class — and R2's
degree-not-kind structure is owned, not repaired: degree is the grade's
content, kind is its declared-divergence boundary. Exact scope:
finite-witness on the T585/T586 fixture class; ω is declared fixture data
(D-conditional per S1); the divergent edge is declared, not derived — the
fixture witnesses the separation, GR supplies the geometry; no general
theorems. Reopeners: continuum/infinite-record versions of the three
checks; the boost-equivariance gate on the contract (the T584 quotient
lacks boosts — covariant note §6, item 4; named, not opened); any
horizon-side derivation replacing the declared edge.

**L3 — PARTIAL, per S4's own offer.** Discharged at fixture level with
priced residues (the two conventions of §1); the continuum halves of the
schema's discharge shape — factorization funded by region algebras,
pointer basis by einselection proper, the sieve functional replacing
C2/C3 — are **named-not-worked**, and the registered kill (both routes
circular ⇒ declared priced primitive) **remains live for the continuum
framing only**. Binding forward obligation, carried verbatim: any arm
attempting route A's continuum framing must first source the QFT
split-property caveat to primary algebraic-QFT literature **or drop the
tensor-factorization framing**; it may not be cited at memory depth in
load-bearing position. Reopeners: the continuum halves; substrate
sensitivity (nothing bounds the horizon convention's growth on richer
fixtures); the grouping family is five members, not a space.

**L4 — SPLIT, per S5.** The precise-failure half and the genuine-exit
half, neither softened:

- **Local Friendliness: EXIT-COLLAPSES — the registered kill fires.** The
  module's graded absoluteness blocks neither No-Superdeterminism nor
  Locality, cannot deny the capability antecedent in kind (N8's promotion
  gate binds the module's own vocabulary), and the fetched formalization
  contains no revelation premise to block. The only stopping point is
  Assumption 1, and stopping there is rejecting it. The premise the module
  rejects, verbatim: **"An observed event is a real single event, and not
  relative to anything or anyone."** The considered position affirms the
  negation at a named grade — value-absoluteness relative to every agent
  class whose envelope cannot un-commit the record, exact absoluteness for
  no finite record outside horizons. The heavier entry, taken honestly; a
  precise failure, and per the charter a successful outcome. This half is
  **unconditional** — it survives even a perfect commit functional.
- **Frauchiger–Renner: EXIT-REAL at (C).** The module's native transport
  rule (record contents are standing premises only while final-for-the-
  receiver) denies FR's (C) as stated and affirms (C)-at-grade; the
  derivation's need for unrestricted (C) at the un-commit-crossing steps is
  verified against the protocol; the account lands in FR's own drop-(C)
  column **with a physical criterion** (envelope containment of the
  un-commit) rather than an interpretive stipulation. **S3-conditional**,
  and the condition is now met at the scope S3 could deliver: S3's kill did
  not fire, so the transport rule has a fixture-level functional to read;
  the residual conditionality is continuum-scope. Price: (C)-at-grade —
  inter-agent consistency grade-relative across supermeasurement
  boundaries.

Reopeners for L4: a continuum failure of S3 reverts the FR half to
unsettled (the LF half stands regardless); the Bong et al. extraction-layer
caveat (§7); observer-scale LF experiments (the thoughtful-LF program's
prospect, not this repo's claim).

---

## 3. Module-vs-module, at full symmetry (the Goal-3 comparison's earned end-state)

Both packages are now priced in named currencies, every Wigner cell named,
quoted, and paid:

| | **foliation + beables** | **causal order + commit module (CM)** |
|---|---|---|
| currency | **ontology** — new physical structure asserted real (preferred foliation; beables per leaf; typicality measure) | **declaration** — context-relative selection over standard descriptions (declared task contexts; the T584-orbit representative; the terminal-record horizon convention; graded identifications) |
| LF trilemma | keeps AOE exact and gradeless; **rejects Locality** | keeps Assumptions 2 and 3 exactly (causal boundedness intact); **rejects AOE-at-grade** |
| FR theorem | **pays at (Q)** (universal Bohmian mechanics drops (Q) — swings' extraction depth) | **pays at (C)-at-grade** (a physical criterion; S3-conditional) |
| added selection structure | the beables entry | D — shape-symmetric to beables (R3), **currency-asymmetric at fixture level** (S4: nothing new asserted to exist) |
| shared imports | the standard thermodynamic record module (Landauer, Crooks, decoherence) — genuinely shared; the issuance *source* question — a shared debt, TI's | same |

The S5 symmetry sentence, verbatim: **"Each package retains exactly the
premise the other rejects, and each carries one added-selection-structure
entry of the same shape."**

The differential is a character difference, not a bookkeeping asymmetry:
exact gradeless AOE at the price of new structure and exact nonlocality,
versus exact locality and no-preferred-structure at the price of graded
absoluteness on unmodified physics. Neither side may any longer present its
Wigner cell as free. The goal2 verdict's not-equal-weight clause stands
unmodified — the rival's debts remain completeness-accounting on cheap,
well-tested standard imports, while foliation+beables remains new physical
structure carrying Goal-2 burdens — and this series sharpens it from a
shape claim to a measured one (S4's currency asymmetry). **No winner is
declared here**: the comparison's earned end-state under S1 §5's SUCCESS
clause is exactly this priced symmetry with exact differential entries;
converting it into a preference is comparative use, which routes through
the goal2 verdict and its owners.

**What this does to the goal2 verdict's §3 clause: STRENGTHENS.** The
clause's "thermodynamic commit module" was a named black box carrying a
cost column. It is now a stated, partially-derived, fully-priced object:
statement (S1), issuance derived-conditional-on-(G, D) (S2), the grade
delivered and checked (S3), the decomposition derived up to a declared
equivalence class at fixture level (S4), the Wigner exit settled at a
named price (S5). The verdict's parenthetical — "posited issuance,
FAPP-grade finality outside horizons, and a **named-but-unpaid**
Wigner-friend exit" — can now cite this series, and its last item is
superseded: the exit is named **and paid**. The proposed one-line §3
update is slate item 5 (§4); nothing is enacted here.

---

## 4. The consolidated enactment slate (proposed, not enacted)

The orchestrator enacts after the closers; owners decide. Every anchor is
given exactly. Two merge decisions are flagged inline so the closers can
check them.

**Slate item 1 — the merged cost-column row 3 (S4's append + S5's
replacement, folded into ONE row).**
Target: [rival-symmetry-swings-2026-07-28.md](rival-symmetry-swings-2026-07-28.md),
section "What the rival map now says," table "The rival's cost column,"
row 3 — the row beginning `| 3 | Wigner-friend exit unpaid until named:`.
Replace the entire row with:

> | 3 | Wigner-friend exit **settled** (S5): **LF — paid at AOE.**
> Assumption 1 ("An observed event is a real single event, and not
> relative to anything or anyone") is rejected in graded form: for every
> finite-ω record, value-absoluteness is relative to the family of agent
> classes whose envelopes cannot un-commit it; exact absoluteness is
> available for no finite record outside causal horizons. Intra-family
> record agreement (all sub-un-commit classes, all mutually final records)
> is retained. **FR — blocked at (C):** promotion of conclusions is denied
> across un-commit boundaries; (C)-at-grade retained; the account lands in
> FR's own drop-(C) column with a physical criterion (S3-conditional;
> condition met at fixture level by S3's landing). The exit's
> decomposition half is priced by S4: on the T58x fixtures D is derived up
> to equivalence (T584 orbit + a terminal-record horizon convention) from
> consumption structure + the declared task context + a grade-stability
> criterion, so "privileged decomposition" re-prices to
> "declared-context-relative selection"
> ([commit-module-s4-decomposition-debt-2026-07-28.md](commit-module-s4-decomposition-debt-2026-07-28.md);
> continuum case named-not-worked). | R3 → S4/S5 | the heavier LF entry
> taken honestly; mirror-symmetric to the foliation side's payment — the
> symmetry restatement is
> [commit-module-s5-wigner-settlement-2026-07-28.md](commit-module-s5-wigner-settlement-2026-07-28.md)
> §6 |

*Merge decisions, flagged:* (i) S5's row ended "(see below)," a deictic
that dangles once the row leaves S5 — replaced with the explicit S5 §6
pointer; (ii) S5 wrote "(S3-conditional)" before S3 landed — the merged
row adds "condition met at fixture level by S3's landing," which is S6's
addition, checkable against S3's verdict block; (iii) S4's append is
folded as the "decomposition half" sentence, with source-swing cell
updated R3 → S4/S5 accordingly.

**Slate item 2 — the earned-form clause (d) insertion (S4's proposal (b),
verbatim).**
Target: same file, same section, the **first** blockquote (the earned-form
blockquote beginning "**GR causal structure + a thermodynamic commit
module** — where the module (a) selects…"). Insertion point: after
"(a named exit, at a named price)" and before the sentence "The causal
layer contributes the access geometry". Insert:

> , and (d) fixes the system/environment split and record coarse-graining
> (D) — at fixture level derived up to a declared equivalence class from
> already-priced ingredients rather than standing as an independent
> privileged structure (S4).

Anchor note for the enactor: S4 described this blockquote as "mirrored in
goal2 §3 / VERDICT.md," but the goal2 §3 and VERDICT.md mirrors carry the
*second* blockquote (the "strongest surviving rival … carrying its own
cost column" sentence, which has no (a)–(c) clauses); the (d) insertion
applies only to the swings' first blockquote. The mirrors are covered by
slate item 5 and the owner-discretion note below.

**Slate item 3 — schema L4 ledger row (per S5).**
Target: [commit-module-schema-2026-07-28.md](commit-module-schema-2026-07-28.md)
§3, "Ledger summary" table, L4 row — the cell "exit (iii) posture,
unsettled". Replace with: "settled SPLIT (S5): LF collapse at Assumption
1, graded; FR real exit at (C), S3-conditional." *Enactor note:* that
table's column is headed "status after S1"; a cleaner enactment (owner's
choice) adds a "disposition after S6" column carrying all four rows — L1
discharged-conditional, L2 discharged, L3 PARTIAL, L4 settled SPLIT (§2's
table) — instead of editing one historical cell.

**Slate item 4 — schema §1.v sealed-labs bullet (per S5).**
Target: same file, §1.v, the bullet "**Sealed coherently controllable
labs.**", after its final sentence ("Whether that posture actually exits
the no-go theorems is Debt L4 / S5, not assumed here."). Append: "Settled
(S5): the posture exits FR at (C); it does not exit LF; the AOE entry is
paid, graded."

**Slate item 5 — the goal2 §3 one-line update (this note's proposal, per
§3 above).**
Target: [goal2-charter-verdict-2026-07-28.md](goal2-charter-verdict-2026-07-28.md)
§3, immediately after the earned-form blockquote (the blockquote ending
"…a shared import on both sides."). Insert:

> *(Commit-module series, 2026-07-28: the module itself is now stated and
> priced — CM = (I, G, D); issuance derived-conditional-on-(G, D) (S2);
> commit(A, r) delivered at fixture level (S3); D derived up to a declared
> equivalence class at fixture level (S4); the Wigner-friend exit settled
> SPLIT — LF paid at AOE-at-grade, FR exited at (C) (S5) — so this
> clause's "named-but-unpaid Wigner-friend exit" now reads: named and
> paid, at the S5 price;
> [commit-module-s6-series-conclusion-2026-07-28.md](commit-module-s6-series-conclusion-2026-07-28.md).)*

**Owner-discretion pointer candidates (noted, not proposed):** VERDICT.md's
mirror phrase "a mandatory named Wigner-friend exit" (a graded standing
surface — any touch is the steward's, post-grading);
[rival-map-2026-07-28.md](rival-map-2026-07-28.md) row 6's "Status
honesty" line (a one-line series pointer would keep the map current).
Routing any of §2's reopeners into
[standing-monitorables-2026-07-28.md](standing-monitorables-2026-07-28.md)
is likewise an owner move, not performed here.

---

## 5. What the series does NOT establish

- **Fixture-locality throughout.** All three probes are finite-witness on
  one Landauer fixture family plus its declared sweeps (S2: 26 checks; S3:
  16; S4: 27 — re-run by this arm, exit 0 each). No general theorems; no
  claim survives transplantation off the fixture class without new work.
- **The continuum/QFT halves are named, not worked.** Route A proper, the
  sieve functional, route B's fixed point, and the infinite-record versions
  of S3's checks are all open; the sourced-or-dropped QFT-caveat obligation
  binds any continuum arm (§2, L3).
- **Interpretation-neutrality is maintained.** The dynamic-unity STOP on
  foliation-existence testing was respected in every arm: nothing in this
  series asserts or tests whether any foliation, update ordering, tick, or
  beable exists (`NI-DU-87` remains DU's truth, by pointer). What "actually
  happen" *means* is not adjudicated anywhere in the series — the module
  grades what happening costs to undo, and stays silent on the metaphysics
  beneath the grade (S5's destructive sub-reading is a steelman, not an
  assertion; landing in FR's drop-(C) column classifies the exit and
  endorses no interpretation). The module's distinctive *ordering* content
  remains interpretation-only in Goal-2's classification sense (S1 §1.iv's
  null, carried unchanged); its quantitative content is measurable but
  standard. That classification consequence, which S1 assigned to this
  conclusion, is hereby stated — and stated as a classification, not banked
  as a promotion.
- **No claim movement.** H2, H3, H4, R1, D2, T3, T583–T588, every claim,
  bin, canon tier, guardrail, and test status stay exactly where their
  owners left them. The Goal-2 NO is untouched.
- **The rival remains Conjecture/Open.** The series priced the rival's
  module; it did not promote the rival, and no tick-family credit flows in
  either direction — the tick side's ten-plus adverse artifacts stand
  regardless of how well the rival's module priced out.
- **The source question is untouched.** Which physics *sources* issuance
  (PP-3/D-FORK) is temporal-issuance's, routed by pointer in every swing;
  S2's reproduction result leaves it exactly as open as before.

---

## 6. Cross-artifact seams (recorded, not smoothed)

The five artifacts were checked against each other for this note. Nothing
found rises to a verdict contradiction; five seams are recorded for the
closers rather than smoothed over:

1. **The copy-step attribution seam (S2 vs S3).** S2's mechanism-honest
   provenance grouping types the copy event as *broadcast* — not an issuer;
   the copy→erase edge basis re-attaches to the seed — and shows
   reproduction fails there. S3's grade tables consume T586's typing, in
   which the copy **is** a producer with its own ω = 6.0 and its own
   un-commit closure row. The two artifacts price the same event under
   rival attributions. Lawful — S3 declares T586's closure a source-owned
   input, and S2's job was precisely to test that typing — but the seam is
   real: the mechanism and the typing disagree exactly at the event that
   creates redundancy, and both notes' numbers inherit their side of the
   disagreement.
2. **S3 §3.6's binary forward branch vs S2's actual PARTIAL.** S3 wrote
   "if S2 lands … if S2 fails"; S2 returned neither. Per S3's own fallback
   clause the order-compatibility check stands on its own; but the "one
   mechanism, two faces" unification S3 sketched is **not earned** — S2
   established that redundancy does no split-robust selective work on this
   fixture class, so the unification holds at most in the D-conditional
   degenerate form.
3. **Two operationalizations of "stable" (S2's (c) vs S4's C3).** S2's
   certified variant requires an in-fixture certification *execution* per
   record (it issues only the erase output); S4's C3 requires carrier-class
   *certifiability* against the declared budget and horizon (it admits all
   five carrier tokens). Both flag stability as grade content — the I→G
   edge — but the predicates differ in granularity and are not
   interchangeable; any downstream use of "certified stability" must say
   which is meant.
4. **The schema ledger's frozen header vs in-place status edits.** The
   ledger table is headed "status after S1"; S5's proposed L4 edit (slate
   item 3) rewrites a cell of a historical snapshot. Recorded with the
   cleaner alternative offered in the slate; the owner chooses.
5. **S5's FR conditionality, written blind to S3.** S5 stated the FR exit
   reverts to unsettled if S3's kill fires terminally; S3's kill did not
   fire — at fixture level. No contradiction, but the scope must travel:
   the FR exit's physical criterion is fixture-grounded and
   continuum-conditional, and any external citation of the settlement must
   carry that rider (plus the extraction-layer caveat, §7).

---

## 7. Next objects, consolidated (named, not built)

1. **The Q-0066 seed — the two-stroke ratchet with a substrate stroke.**
   Registered wiki-first 2026-07-28 on the private thinking-wiki surface
   (Q-0066; pointer only, per public-repo hygiene; ownership per the
   registration — TI owns the fork, TaF the currency, DU seam-only). The
   TaF-native objects, in cost order: the **T584 type-extension morphism
   packet** (T584's morphism classes contain no type-extension class — the
   cheapest formal object, same T587-compliant packet discipline as the
   composition gate); the **ratchet toy with S3's finality grades as the
   substrate parameter** (prediction within a fixed type space vs
   forecasting under type-space growth, with commit(A, r) supplying the
   settlement-substrate grade); the **Still et al. + predictability-sieve
   N-note import** (upgrading S4's recalled-depth Still–Sivak–Bell–Crooks
   flag and N7's shelf-depth sieve to a sourced literature note before any
   load-bearing use).
2. **The S2 reopener: a richer frozen fixture family** — multi-consumer,
   conditional-state, priced-copy — the only venue where SBS's
   discriminating structure (R ≫ 1, strong independence,
   distinguishability) is expressible, and simultaneously the genuine test
   of repair route 1 (threshold on G given D), which this series never ran.
3. **S4's continuum halves** — route A proper (region-algebra
   factorization; einselection; the sieve functional) with the L3 kill
   still registered there, under the binding **sourced-or-dropped QFT
   caveat obligation** (§2, L3).
4. **The boost-equivariance gate** (covariant note §6, item 4): the T584
   quotient lacks boosts; declare the isometry morphism class, require
   envelope equivariance and scalarization invariance, fail-closed checks
   on the corner-distance budget and equal-β composition restriction.
   Opening requires an owner decision under T587's discipline. Directly
   relevant to L2's invariance clause at covariant scope.
5. **The Bong et al. extraction-layer caveat** (S5's provenance): every
   quoted LF assumption, Methods condition, and protocol sentence is ar5iv
   extraction-fidelity, not eye-verified against rendered LaTeX. **If the
   settlement is ever cited externally, verify the quotes against the
   published PDF first.** Internal use carries the flag as S5 states it.
   *(Adversary-close update, same day: the four load-bearing LF quotes and
   FR's (Q)/(C)/(S) + theorem were verified against the arXiv PDF — ar5iv
   renders FR's assumption boxes empty, so S5's bracketed content was
   reconstruction, now verified content-faithful; the standing check
   narrows to the journal-typeset versions. Recorded in S5's Provenance.)*

Standing dependency (not an object): the **DU-holdings gate arm report**
(§2, L1) — until it is consulted, S2 is complete but not closed.
*Resolution: the gate ran report-only — its GO/SEAM verdicts are recorded
in the series' closeout mailbox note on the system-runtime surface; cite
forward to that note as the consultable record (no in-repo report will
appear).*

---

## What This Does Not Claim

- **No enactments.** Every edit in §4 is proposed; the orchestrator enacts
  after the closers; owners decide. This note's only write is itself.
- **No claim movement, no T-numbers, no bin regrades** — restated from the
  header because it binds every section above.
- **No winner on Goal 3.** §3 executes the comparison at its earned
  symmetric form and stops; comparative preference is owned by the goal2
  verdict's surfaces and their owners.
- **No rival promotion and no tick-family credit.** H3 remains a
  Conjecture; the rival remains Conjecture/Open; the adverse tick artifacts
  stand.
- **No new physics and no priority.** Landauer, Crooks, Jarzynski,
  einselection, quantum Darwinism, SBS, Bong et al., and Frauchiger–Renner
  are their authors'. The series' contribution is application-level: a
  schema, three deterministic fixture probes, and an adjudication
  discipline. Priority is claimed for nothing.
- **Depth limits are binding and carried.** Crooks at the swings'
  full-text fetch; Bong et al. and FR at S5's extraction fidelity;
  thoughtful-LF at abstract depth; the sieve at shelf depth; Still et al.
  at recalled depth, load-bearing nowhere; the QFT caveat memory-cited and
  unconsumed. No depth was upgraded by this note.
- **Steward grading of the series is a separate open step.** This
  conclusion is the series' own account of itself; grading it against the
  practice instruments (including whether instrument 12's anatomy is met)
  belongs to the steward, not to this arm.

## Provenance

- **Writer lock:** checked before writing —
  `git rev-parse --git-path capacityos-writer.lock` →
  `.git/capacityos-writer.lock`, not present. HEAD at read and write time:
  `1db0f6a` (the series' declared baseline), with the five sibling
  landings present uncommitted in the working tree (5 notes + 3 probe
  models, all read). This arm performs file writes only; no commit, no
  push; this file is its only write.
- **Series record read in full:** S1, S2, S3, S4, S5 notes and all three
  probe sources (`redundancy_issuance_probe.py`,
  `capability_graded_finality_probe.py`, `decomposition_sweep_probe.py`).
- **Probes re-executed by this arm** (from the repository root, bytecode
  writes suppressed so the tree stays clean): S2 26/26 gated checks match,
  S3 16/16 pass, S4 27/27 match; exit 0 each. Numbers quoted in §§1–2 are
  from the siblings' notes, spot-confirmed against these runs.
- **Comparison surfaces read:** rival-symmetry-swings (fully — the cost
  column, the earned-form blockquote, R1–R3, the caveat section);
  rival-map (fully — row 6); goal2 §3–§4; VERDICT.md (the rival
  sentence); adversarial-research-practice instrument 12 (+ its goal2
  exemplar anatomy); covariant note §6 (slate/gate anchors);
  goal2-charter-verdict-grading (status only).
- **Wording-freshness check (read-only, private surface, pointer only):**
  the two pending P1 notes in the system-runtime TaF mailbox
  (`20260728-strategy-wave-consolidated.md`;
  `20260728-wave3-consolidated-verification-package.md`) were read in
  full. The strategy-wave note's E4 bullet restates the rival's earned
  form consistently with the in-repo wording; the wave-3 package's
  annotations touch V1/V3 and the two named gates only. **No steward
  annotation changes the rival's earned wording**; the in-repo wording is
  used throughout this note.
- **DU-holdings gate arm report:** searched for before writing (TaF
  explorations and agent-run surfaces; DU explorations; system-runtime
  mailboxes) — **absent at write time**. Carried as L1's open dependency
  (§2), per the schema's boundary sentence 2 and S2's provenance; not
  discharged here.
- **Q-0066 registration:** verified present on the private thinking-wiki
  surface (question row + exploration note) before citing; cited by
  pointer only, per public-repo hygiene; no private content beyond the
  registration's existence and ownership line is imported.
- **No fetches this run.** All external physics enters via the siblings'
  labeled fetches and N-note listings; every depth label is carried, none
  upgraded.
- **No new kills.** This note registers nothing and fires nothing; every
  verdict above is a sibling's, quoted at its earned scope, with the two
  §4 merge decisions and the §6 seams flagged as this note's own
  editorial acts.
