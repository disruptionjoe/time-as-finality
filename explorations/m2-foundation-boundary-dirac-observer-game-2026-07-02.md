---
document_type: exploration_foundation
primary_reader: agents
read_pattern: current_state
authority: exploratory
created: 2026-07-02
status: exploratory
source: >
  explorations/meta-synthesis-reverse-pass-2026-07-02/SYNTHESIS.md (M2, next-move #2);
  results/T422-representability-span-v0.1-results.md (the category-relativity ceiling M2 must beat);
  cross-repo scout: gu-formalization, temporal-issuance, ai-epistemology
---

# M2 Foundation — Exhibit the Object Whose Index/Value IS the Finality Datum

## Purpose (prep, not a swing)

Set the foundation for **M2** (meta-synthesis next-move #2): move the recurring
"global section with no local witness" object (finality = legitimacy = the
capability separator) from **rhyme to computed** by exhibiting a *concrete*
boundary-Dirac operator OR non-atomic observer-game whose **index / value IS** the
finality datum. No swing here; this scopes the routes, harvests reusable
templates, and fixes the bar. No claim movement; ledger untouched.

## The bar M2 must clear (inherited from T422)

T422 delivered the span at **finite-witness synthesis tier** but its core was
**category-relative** — there was *no single canonical category* in which E1/E3
are representable and E2 is not. **M2's whole job is to beat that: exhibit a
CANONICAL object** (an operator or a game) whose index/value *is* the finality
datum independent of a hand-picked category. Canonicity is the success bar; a
category-relative object is a REDESIGN.

**The canonicity strategy this scout surfaced:** the same "global-no-local" object
appears — independently — as three established formalizations:
1. the **APS boundary eta-invariant** of a Dirac operator on a manifold *with
   boundary* (index = interior integral + a boundary term that is a genuine
   global-no-local datum);
2. the **Aumann–Shapley non-atomic value** (a global datum carried by no finite
   coalition — TaF's own T413 lineage);
3. the **judgment-aggregation collective judgment** (List–Pettit): a group verdict
   that provably cannot be reconstructed from local individual judgments (the
   discursive dilemma) — the social-epistemology form of "no local witness."
**If a single M2 object is simultaneously an APS-eta term, an Aumann–Shapley value,
AND a judgment-aggregation obstruction, that triple-coincidence is the canonicity
T422 lacked** — the object is not category-relative because three independent
categories compute the same thing.

## Route A — Boundary-Dirac / APS-eta (index route)

Build a finite TaF-side **boundary-Dirac operator** whose index — specifically its
**APS boundary eta term** — IS the finality/capability separator. The boundary eta
invariant is exactly a "global datum living at the boundary with no local witness"
(item-10 object), realized as an index term.

**Reusable TEMPLATE (adjacency only — see one-way rule):** GU's
`analytic-index-fredholm` / `function-space-ext` line already builds this shape:
- `gu-formalization/tests/function-space-ext/aps_eta_boundary_control.py` +
  `explorations/analytic-index-fredholm/function-space-aps-eta-boundary-control-2026-07-02.md`
  (APS/noncompact-end eta boundary control).
- `.../dirac_spectral_flow_section.py` (net chiral spectral flow = index, section-set).
- `.../krein_spectral_flow_probe.py` (Krein/ghost-parity spectral flow — ties the
  T421 metric-E3 side and the ghost-parity thread).
- `.../flux_index_2d.py`, `canon/function-space-index-conservation-RESULTS.md`.
These are **structural template and vocabulary, NOT importable evidence.** M2 must
build its OWN finite operator on TaF's capability/record structure and *reproduce*
the boundary-index shape, not cite GU's numbers.

## Route B — Non-atomic observer-game / judgment-aggregation (value route)

Build the **non-atomic observer-game** whose **Aumann–Shapley value** IS the
finality/legitimacy datum, using **judgment aggregation** (List–Pettit) as the
finite discrete backbone (the discursive dilemma is the finite witness; the
non-atomic limit is the "legitimacy at the infinite layers" of T413).

**Reusable TEMPLATE + adjacency:**
- TaF-native: T413 (`results/T413-legitimacy-shapley-finality-probe-v0.1-results.md`)
  already built the finite cooperative-game separator and the Aumann–Shapley trend.
- **ai-epistemology** (sibling; adjacency): an entire **legitimacy-stability**
  formalism (`explorations/E006/E007/E008/E016/E017` — legitimacy stability, founding
  vs operating legitimacy, epistemic inflation, calibration-independence) AND the
  **collective-intelligence / quadratic / judgment-aggregation** absorber
  (`absorbers/collective-intelligence-quadratic-aggregation.md`; List, Pettit, Weyl,
  Lalley–Weyl). This is the same "global-no-local collective object" M2 wants,
  already formalized in the social-epistemology register — the strongest reusable
  scaffold for Route B.
- temporal-issuance (adjacency): `explorations/E125-capability-typed-ext-s-surplus-test`
  and the tri-repo capability-burden retyping — the observer/value/issuance side.

## Cross-repo scout — what each sibling contributes (all ADJACENCY, one-way)

- **GU:** the boundary-index template (APS-eta, Dirac/Krein spectral flow). *Its
  own `time-as-finality-crosswalk/README.md` already fixes the discipline:* allowed
  = sharpen observer/readout legs, separate record propagation from final readout,
  add bounded tests; **forbidden** = treat finality as a physics proof, treat
  observer agreement as a replacement for anomaly/chirality/spectral-triple, claim
  no-gos bypassed, import a hidden global commit order. M2 honors this verbatim.
- **temporal-issuance:** the observer/value/issuance adjacency (capability-typed
  Ext_S surplus); the value M2's game must not silently import as a TI claim.
- **ai-epistemology (the "just for fun" find that landed):** legitimacy-stability
  theory + judgment aggregation = the discrete, already-formalized cousin of the
  non-atomic observer-game. The **discursive dilemma** (a collective judgment
  reconstructible from no set of individual judgments) is *the same H¹
  "global-no-local" object* the whole program keeps hitting — in the
  social-epistemology register. Route B should borrow its scaffolding.
- **architecture-of-legitimacy** (repo, not yet scanned): flagged as a further lead
  for the legitimacy-as-global-object thread — scan before a Route-B swing.

## The unifying claim this foundation sets up (for the eventual swing)

> The finality datum is a **canonical global-no-local invariant** that is
> *simultaneously* an APS boundary-eta term (index route), an Aumann–Shapley
> non-atomic value (game route), and a judgment-aggregation obstruction (social
> route). Exhibiting one finite object carrying all three descriptions would beat
> T422's category-relativity (three independent categories agree ⇒ canonical) and
> lift M2 from rhyme to computed.

## Success / kill bar for the M2 swing (predeclared here)

**Success:** a finite object (operator or game) whose computed index/value IS the
finality/capability separator, shown canonical by at least two of the three
independent descriptions computing the SAME datum (not a hand-picked category).
**Kill:** the object is only computable in one hand-picked category (category-
relative, as T422) → REDESIGN; the three descriptions compute *different* data
(no common object) → the triple-coincidence is coincidence, ABANDON to a logged
disanalogy; any sibling result is load-bearing as *support* → one-way violation,
rebuild import-free.

## Guards

One-way rule strict: GU / TI / ai-epistemology / architecture-of-legitimacy are
**adjacency, template, and vocabulary — never evidence** for a TaF claim; M2 builds
its own object. Recorded-tier; no claim promotion; no ledger/TESTS.md edit; single-
process ceiling; honest ceiling (finite-witness + canonicity argument, not a
universal theorem). Prep only — the swing pauses for Joe's go.

---

## ADDENDUM 2026-07-02 -- M2 swing run (Route B): outcome = REDESIGN

The M2 swing was executed via **Route B** (finite discrete backbone: List-Pettit
discursive dilemma / doctrinal paradox as the observer-game). Recorded at
`results/T423-m2-observer-game-v0.1-results.md` (model `models/m2_observer_game.py`,
18/18 tests pass, exact `Fraction` arithmetic, n=3 fully enumerated). Only code
import is TaF-native T413; one-way rule held (verified by static + runtime import
audit).

**What landed (survives adversarial audit).**
- **Genuine dilemma.** Frozen profile p=(1,1,0), q=(1,0,1), r_i=p_i&q_i=(1,0,0):
  judges individually consistent, collective inconsistent (maj-p=maj-q=1 accept r
  via premises; maj-r=0 rejects r). Strict 2/3 majorities, no tie artifact.
- **Global-no-local (atomic k=1).** The gap game v_gap = P(S)-C(S) is EXACTLY the
  unanimity game u_N: 0 on all 7 proper coalitions, 1 only on N. SURVIVES-R1;
  collective verdict reconstructible from NO proper subset. *Caveat:* this strict
  no-local-witness property is an **atomic knife-edge** -- it FAILS under k>=2
  replication (a one-of-each-type size-3 sub-coalition reproduces the dilemma). The
  non-atomic limit preserves only Delta = v_gap(N) = 1 with vanishing atoms.
- **Aumann-Shapley IS the separator.** phi = (1/3,1/3,1/3), d(N)=1 IS T413 Pair-2's
  SURVIVES-R1 grand-dividend (structurally, not a fresh number). Two independent
  Shapley routines agree; a rigged u_12 decouples value-sums-to-1 from
  datum-location, proving SURVIVES-R1 tracks location, not efficiency.
- **Finality-specific cross-application un-pre-empted** (prior-art partial; see
  below).

**What did NOT land -- the predeclared REDESIGN kill fired.** The M2 target was
**canonicity**: one datum computed by TWO independent descriptions. The two on
offer -- coalition-lattice Shapley d(N) and proposition-lattice MI-set omega -- both
equal 1, BUT `v_gap(S) == dilemma_indicator(S)` as set functions over all 8
coalitions: **one majority primitive read twice, not two channels.** Off the frozen
fixture the readings diverge (collective triple (0,0,1): omega=2, indicator=1), so
the same-datum equality is a category-relative point coincidence. This is exactly
the kill this prep note predeclared -- *"only computable in one hand-picked category
(category-relative, as T422) -> REDESIGN."* **T422's ceiling recurs.**

**Prior-art (partial).** The generic "Shapley value of an inconsistency /
MI-set obstruction measure" connection is pre-empted (Livshits & Kimelfeld,
arXiv:2009.13819). The finality-specific piece -- discursive-dilemma gap-game value
with the no-local-witness / non-atomic-legitimacy framing -- is not pre-empted
(~7 targeted searches).

**Verdict: REDESIGN, not GO, not ABANDON.** Keep the finite witness + the
cross-application (synthesis tier, method not evidence). To reach GO, the canonicity
leg must be rebuilt on a second description that does **not** reduce to the
coalition-majority primitive -- a genuinely independent channel (e.g. the optional
Route-A H^1/index reading, provided it is not a set-function relabel of the majority
verdict). Absent that, the finality datum is **finite-witnessed but not canonically
computed.** Recorded-tier; no promotion; TESTS.md / CLAIM-LEDGER.md untouched;
committed as recorded-tier in `68f814b`. The Route-A redesign lane remains a
separate follow-up; no claim move follows from T423.

## Addendum — Route A swung (T424): REDESIGN. The wall is unified.

Route A (the index channel — the one the Shapley-linearity lemma does not touch)
was built as the decisive fiber-variation test (T424,
`results/T424-m2-route-a-index-probe-v0.1-results.md`; 23 tests; recorded-tier;
TESTS.md/CLAIM-LEDGER.md untouched; one-way import audit in-suite). Three design
agents converged on the diagnosis; the build confirmed it.

**Verdict: REDESIGN (no GO) — but strictly stronger than Route B, with the wall
now unified across all three swings.** On the n=3 dilemma class (2 v_gap fibers):
- **I_chi (combinatorial-Hodge Euler char) and I_fr (signed-graph frustration)
  GENUINELY ESCAPE the relabel bar** — both are non-constant on the verdict's
  fibers, i.e. genuinely independent channels, *which Route B could never produce*
  (Shapley linearity forbade it). This is the real earn.
- **But neither equals the finality separator across the class.** I_chi=1 collides
  (carries both sep=0 and sep=1); I_fr fires on all 6 dilemmas (necessary) but also
  18 non-dilemmas (not sufficient). I_sf (spectral flow) is **null at n=3** — the
  twisted triangle Dirac is provably gapless (`det B(θ) = -1 + P·e^{iθ}` hits the
  origin), so its only residue re-reads the frustration cocycle. (Honest caveat:
  I_sf is null *at this witness size*, not proven null in general.)
- Guard-the-guard PASSED both directions, so the test is trustworthy. Extra
  finding: I_fr's independence is **doctrine-specific** — in the XOR class it
  degenerates to a genuine relabel (Horn 2).

**The unified wall (the durable result).** Canonicity fails because **independence
and separator-agreement are in tension**: on a finite combinatorial object, any
invariant that *equals* the finality datum is a function of the majority verdict (a
relabel — T423's Shapley-linearity lemma, index-side), while any genuinely
*independent* invariant computes *different* data. You get one, not both. Same wall
as T422 (category-relativity) and T423 (shared-primitive), now shown a third time
from the index direction — **one structural reason the "global-no-local finality
object" resists canonical finite-witness computation.** Escape needs a genuine
continuum (an SSH Brillouin circle / a spin structure), and *choosing* it is exactly
T422's hand-picked category.

**Net for M2.** The finality datum is **finite-witnessed but not canonically
computed** by the value route (T423) or the index route (T424) — the honest ceiling,
now with a proof-shaped diagnosis rather than a bare negative. No claim promotion;
ledger untouched; pauses for Joe.
