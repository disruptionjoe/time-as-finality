# RESULTS: finality-native generator dimension-collapse sweep -- PARTIAL (real collapse, 1+1 not validated)

2026-07-10. Executes the frozen spec `records-native-generator-PREDECLARED-SPEC-2026-07-10.md` (committed
first). Model: `models/records_native_generator_sweep.py` (exit 0, deterministic seed 20260710). Screen
grade. **Verdict: PARTIAL** -- a genuine dimensional collapse occurs, but it is not validated as 1+1 and
it requires inertial issuance. No S1 status change.

## What the sweep found

**iid and diffusive issuance FALSIFY.** For both, the causal mask `||dr|| <= c*dt` engulfs bounded /
diffusive records into a chain: `d_hat -> 1` at every `c >= 1`, no stable non-chain plateau. A
finite-propagation causal mask alone does NOT generate geometry from generic records.

**Ballistic issuance produces a genuine dimensional COLLAPSE with a clean crossover** (the robust
finding, independent of absolute calibration):

| c (cone width) | d_hat over D = [2, 4, 8, 14] | reading |
|---|---|---|
| 0.5 | 4.43, 7.70, 8.90, 12.0 | **tracks D** -- the causet resolves the full feature dimension |
| 1.0 | 2.91, 3.19, 3.55, 3.86 | tracks D (weaker) |
| 2.0 | 1.76, 1.92, 1.93, 1.89 | **D-INDEPENDENT (collapsed)** -- 14 columns do not appear as 14 dims |
| 4.0 | 1.46, 1.42, 1.44, 1.46 | D-independent (heading toward chain) |

As the cone widens, `d_hat` stops tracking the column dimension and lands on a low, essentially
**D-independent** value. The 14 feature columns wash out -- the frame's core mechanism ("geometry from
finite-speed resolution of records") is vindicated at screen grade: *something* collapses the feature
dimension.

## Why it is only PARTIAL (three caveats that block the strong claim)

The frozen numeric criterion (`d_hat in [1.7,2.3]`, stable in N, below D) was literally met by three
ballistic cells (D=4/8/14 at c=2). But it is **not** a validated "generator found," for three honest
reasons:

1. **The instrument control FAILED.** A genuine D=1 (1-space + 1-time) process should read `d_hat ~ 2`;
   in this ballistic / slab setting it reads 1.0-3.1 depending on c, never a clean 2. So the absolute
   `d_hat` scale is **uncalibrated** here (the MM estimator is calibrated on uniform diamond sprinklings;
   these causets are non-uniform ballistic-worldline slabs). "It is exactly 1+1" is **not established** --
   only that the collapse lands somewhere low.
2. **It requires ballistic / INERTIAL issuance** -- exactly the predeclared "smuggled-metric" crux.
   Constant-velocity worldlines are most of Minkowski's structure, so the geometry appears precisely when
   inertial motion is put in. The deflationary reading (ballistic = Minkowski smuggled) has real force;
   the positive reading (the frame *recovers* inertial structure as the condition for geometry) is
   available but not privileged.
3. **The low value is a CROSSOVER, not a validated plateau.** `d_hat ~ 1.9` at c=2 sits between
   "resolve-D" (small c) and "chain" (large c). Its N-stability is real, but that it is a genuine 1+1
   manifold dimension -- rather than the crossing point of two regimes -- is not shown.

## Net (honest)

- records-as-rows is **more than a pure relabel** -- a real dimensional collapse (D-independence under a
  wide cone) genuinely occurs -- and **less than a validated generator** -- the collapse imports inertial
  structure and is not pinned to 1+1.
- This **sharpens** the T526 gap rather than closing it: a dimension-reducing ingredient exists (ballistic
  issuance + wide cone), but the honest missing object is now more specific -- a *non-inertial*,
  finality-native issuance rule whose collapse lands on a *validated* manifold dimension, with a
  calibration that survives its own D=1 control.
- **No S1 movement.** S1 stays `requires_added_assumption`. Screen grade; ordering fraction is one
  necessary-not-sufficient statistic; the absolute dimension is uncalibrated for these causets.

## What would upgrade it (concrete, bounded)

1. **Fix the calibration**: re-calibrate the MM estimator on time-extended SLABS (not just diamonds) and
   on the actual ballistic-worldline ensemble, so the D=1 control reads a clean 2 -- then the c=2 collapse
   value can be honestly identified (is it 2, or the crossover?).
2. **Attack the inertial-smuggling crux**: test whether any NON-ballistic (non-inertial) native rule --
   e.g. an interaction/selection rule on the records, not imposed constant velocity -- produces the same
   D-independent collapse. If yes, the collapse is native; if only ballistic works, the frame imports SR.
3. Only then is the chirality leg (discrete Dirac on a survivor's causal graph, frontier boundary,
   net chiral edge mode) worth running -- it needs a validated geometry to sit on.

## Not claimed

Not a spacetime derivation, not a metric, not a validated 1+1, not an S1 promotion, and not a completed
generator. A screen-grade sweep with a predeclared criterion whose honest outcome is: a real but
inertial, uncalibrated dimensional collapse -- the T526 gap sharpened, not closed.
