# W192 Reciprocal Capability Packet Intake

## Status

Cross-domain packet intake and Action-4 preregistration. No claim, Canon Index,
North Star, public posture, TAF4, TAF8, or external publication movement.

## Purpose

Apply the region-indexed capability and reciprocal-bridge contracts to frozen
`gu-formalization` W192. T580 closed further abstract absorber expansion without
new input. W192 is a new concrete cross-domain packet, so this intake applies
the existing absorbers rather than opening another panel.

## Exact object and task menu

Use the written complex spinor proxy

```text
Y_W = (Spin(9,5), H, S, K, Phi_H, C_T^spin)
Y_- = (Y_W, no accessible psi)
Y_+ = (Y_W, accessible frozen psi)
```

The native `Sp(32,32;H)` current and `Phi_ad` are absent. No proxy result is
transferred to that native construction by name.

For the fixed proxy menu:

| Task | `C_R(Y_-)` | `C_R(Y_+)` |
| --- | ---: | ---: |
| nonzero state-independent equivariant selector | 0 | 0 |
| given accessible spinor `r`, form record-provenanced `L_r` | 0 | 1 |
| given `v`, return `x` with `Phi_H x = v` | 1 | 1 |
| locate/evaluate the written-proxy critical shell | 1 | 1 |
| source and measure native current retarded response | untyped | untyped |

Only the second task changes, and its input signature explicitly adds `psi`.
Surjectivity means carrier lifting and the proxy shell already exist in both
cases.

## Boundary and same-data audit

The observer section `H` is an algebraic section, not a TaF control region or
causal boundary. W192 supplies no causal order, observer worldlines, detector
cadence, resource ledger, record-generation law, overlap/gluing, gauge quotient,
or transport rule for `psi`.

Therefore:

- if `psi` is visible, the before/after shadows are unequal;
- if it is hidden but powers interventions, the access profile is incomplete;
- if it arrives from outside, the physical path and forcing law are absent;
- a fixed richer source with delayed access reproduces the proxy transition.

The same-neighbor-data freeze and physically forced-boundary test cannot pass.

## Absorber result

| Absorber | Verdict |
| --- | --- |
| enlarged state/readout | absorbs |
| explicit state/resource or description bit | absorbs exactly |
| joint record | absorbs if another holder supplies `psi` |
| explicit finality label | absorbs if frozen/admitted is the separator |
| causal domain | untestable; declared-boundary default |
| transition-system menu | absorbs if `L_psi` is enabled by declaration |
| latch/substrate | absorbs at current frozen-state granularity |
| fixed source | absorbs the entire proxy transition |
| resource preorder/competency | absorbs the one changed task profile |
| gauge/basis | open until the native quotient exists |
| spectral/dynamical | blocks any physical-survival reading |

## Strongest residue

Inside the written proxy, the exact E3-style residue is

```text
Hom_G(H, H tensor S) = 0,
L_psi != 0,
Phi_H^-1(L_psi(H)) != empty.
```

This establishes state-conditioned selection in that proxy. It does not
establish a capability boundary because `Y_+` is given an additional resource
that already belongs to the fixed source.

## Verdict

```yaml
packet: gu_w192_frozen_spinor_proxy
physical_C_R_difference: not_earned
current_difference: explicit_state_resource_completion
physically_forced_boundary: absent
same_shadow_all_interventions_certificate: absent
native_current_task: untyped
TAF8_movement: none
claim_or_canon_movement: none
```

## Action-4 capability ledger

Freeze this ledger before Action 2 is tuned.

### Cases

`Y_-` and `Y_+` must have identical native law, region, intervention menu,
resources, causal data, gauge convention, detector model, and record map except
for the hypothesized law-forced record event.

### Controls

1. Explicit-state positive control: supply `psi` and recover `L_psi`.
2. Surjectivity control: supply arbitrary `v` and lift it without `psi`.
3. Symmetry control: distinguish joint transformation of `(h,psi)` from
   transformation of `h` alone.
4. Fixed-source control: place every admissible `psi` in a richer source with
   delayed access and attempt to reproduce all tasks.
5. Native-construction control: replace the written proxy with the actual even
   K-anti current and `Phi_ad`.

### Observables

- equality under all region-supported observations and interventions;
- fixed task-success vector and resource costs;
- source and record provenance;
- adapter rank, fibers, and gauge quotient;
- retarded `rho_J`, pole and threshold data;
- perturbative persistence and record preservation.

### Completion attacks

Apply readout, explicit state, joint record, finality label, causal domain,
transition system, latch/substrate, law sector, resource/competency, fixed
source, gauge quotient, and spectral stability attacks.

### Pass rule

All predeclared region shadows and interventions remain equal; the menu is
identical; a native law forces the record event; every legitimate fixed-source
and completion model fails; and at least one native physical-response task
changes and persists.

### Fail rule

Fail if the changed task is decided by adding `psi`, an availability or
finality bit, a changed menu, joint access, ordinary causal reachability, a
fixed richer source, or if the native current and retarded response remain
absent.

## Next route

The ledger can be implemented as a fixture now. A positive capability verdict
must wait for the typed Action-2 source law and the Action-3 noncompletion test.
