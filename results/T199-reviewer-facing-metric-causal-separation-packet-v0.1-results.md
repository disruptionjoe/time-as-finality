# T199 Results: Reviewer-Facing Metric-Causal Separation Packet

## Correction Notice

Superseded/narrowed by T208. This packet is safe only as a finite
harmonic-proxy packet. It should not be exported as an exact Moses,
arbitrary-DAG-flow, or continuum WBE result.

## Outcome

`narrowed`

The reviewer-facing export packet is now available, and it is intentionally
narrow.

## Packet Verdict

The strongest safe export is:

```text
finite metric-causal separation survives;
non-tree finite extension exists;
predictive content collapses to T* / T*_DAG;
independent capability residue is absorbed;
continuum WBE support is not yet earned.
```

## Why This Is The Right Export Shape

It keeps the three most important Wave 3 facts together:

1. T195 prevents the result from being dismissed as only a tree artifact.
2. T197 prevents the capability story from being exported as an independent
   theorem.
3. T196 prevents continuum WBE language from being smuggled in as if already
   bridged.

## Reviewer-Safe Claim

The packet supports a finite claim, not a grander one:

```text
In the tested finite weighted families, causal order alone does not determine
the finite Moses-style exponent proxy; metric delivery-time structure can change
it, including on tested non-tree DAG fixtures.
```

## Reviewer-Safe Non-Claims

The packet explicitly excludes:

- arbitrary-DAG generality;
- independent Cap_TI capability promotion;
- continuum WBE derivation;
- physical-arrow or H7 upgrade.

## Export Readiness

For its intended role, T199 succeeds:

- the core finite claim is compact;
- the narrowing and absorption story is explicit;
- the non-claims are front-loaded instead of buried.

So the packet is ready as a reviewer-facing summary of what survived this
fanout, provided it is used with those non-claims intact.
