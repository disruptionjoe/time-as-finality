# T411 Departed-Record Capability Discriminator — Results v0.1

- **Artifact:** `T411-departed-record-capability-discriminator-v0.1`
- **Spec:** [tests/T411-departed-record-capability-discriminator.md](../tests/T411-departed-record-capability-discriminator.md)
- **Model:** [models/departed_record_capability_discriminator.py](../models/departed_record_capability_discriminator.py)
- **Test:** [tests/test_departed_record_capability_discriminator.py](../tests/test_departed_record_capability_discriminator.py)
- **Numbers:** [T411-departed-record-capability-discriminator-v0.1.json](T411-departed-record-capability-discriminator-v0.1.json)
- **Tags:** departed_record_capability_discriminator,
  region_indexed_capability_states,
  interventional_equality_lemma_carried,
  boundary_crossing_menu_separation, forcing_certified_inside_R,
  departure_carried_completion_datum, positive_control_priced,
  absorber_battery_run, no_claim_promotion

**Hostile review is QUEUED for this artifact, not yet performed.**
Nothing below has survived independent adversarial reproduction. T409
is cited in its corrected, re-scoped, internally-established standing
(`audits/2026-07-02-t409-hostile-review.md`). **T410 — whose model this
artifact imports as machinery — is recorded tier only (its hostile
review is QUEUED)**; every T410-derived fact used here is re-asserted
inside this artifact's own suite, and nothing is taken on T410's
authority.

**T-number provisional:** T411 verified free at every file-creation
step (highest taken anywhere: T410; the only prior "T411" hits were
T410's next-free note and a steward run recommending "a T411-style
absorber audit" with numbering explicitly provisional — no reservation).
Next free T-numbers as of this write: **T412+**. Noted here only;
TESTS.md and CLAIM-LEDGER.md untouched per house rules.

**Predeclaration trail:** file-creation order was spec → model → tests
→ results (this file last); the spec was content-frozen before the
model file existed and has not been edited since. Constants imported
byte-identical from T392/T393 (committed) and T410 (working tree) are
cited in the spec; every T411-specific sweep, threshold, tolerance,
seed, intervention family, and failure leg is in the frozen spec. All
predeclared corner values below were hand-derived closed forms at
spec-freeze time; none is a run output quoted back into the spec.

## Verdict (house vocabulary)

**The departed-record capability discriminator holds in this finite
family — all eight legs.** Two capability states on T410's thermal
collision substrate — A (which-way record broadcast into `m <= 3`
retained Gibbs(`beta`) carriers) and B (the same broadcast, then each
carrier's content swapped into a fresh tier-2 carrier that DEPARTS) —
are **exactly equal under all R-supported statistics, observational and
under every R-supported intervention**, the ALL quantifier carried by
the environment-side channel lemma (`B = (id_R (x) Lambda_E)(A)`,
`Lambda_E` on tier-1 factors only) and asserted operator-level plus on
the predeclared operational family (12 unitaries, 2 instruments, a
sequential composition, seeded Haar rows — illustrative), everything at
machine floor. One fixed boundary-crossing menu separates them:
**A is `restorable-at-R+`** (un-write restores
`vis_A = 4 sqrt(3)/7 = 0.989743 >= v* = 0.9` at every `beta` and `m`)
and **B is `final-relative-to-R+`**, the final verdict carried by the
exact phi-independence certificate against ALL channels with unlimited
work, pure or Gibbs (B's retained state is an exact product
`rho_R (x) tau_beta^(x)m` — the joint-record completion returns
nothing). Crossing is **forced**: the task is certified impossible
inside `R` for both states. The completing datum — dispersal depth —
departed with the traced-out tier-2 carriers; at `beta = 0` **no proper
subset of the reach holds the datum at all** (all 200 enumerated
proper-subset marginals identical; full joints differ by trace distance
exactly `vis_A/2`). Enlargement restores capability and is **priced**
(the positive control at `R++`, with the cascade ledger). This realizes
the open problem's success criterion at the declared `R`, in this
finite fixture, relative to the declared reaches and menu —
**finite-witness**. The boundary is physical GIVEN the
repeated-interaction reservoir idealization, which is **adopted, not
derived** (T407's standing objection is NOT claimed discharged
unconditionally); the transport rung — work literally buying reach, an
unconditional physical boundary — stays **named-unbuilt**. Whether this
fixture discharges the open problem is a review decision. **No claim
promotion; no CLAIM-LEDGER entry; ledger actions pause for Joe per
AGENTS.md.**

The open problem's success criterion, quoted and mapped:

> No functional of R-supported statistics, including statistics under
> all declared R-supported interventions, separates the two capability
> states; but a boundary-crossing task menu separates them.

| Demanded ingredient (open problem) | Carried by |
| --- | --- |
| exact equality certificates, observational | Leg 1 |
| exact equality certificates, interventional | Leg 2 (lemma + family) |
| boundary-crossing menu changing recovery capability | Leg 4 |
| crossing forced, not merely admitted | Leg 3 (all-channel certificates at `R`) |
| positive controls: enlargement restores | Leg 5 (priced) |
| T399/T400/T401/T402–T406/T397/T398-T404 absorber checks | Leg 7 (named controls) |

## Predeclarations (fixed before inspecting numbers)

- Imported unchanged and cited: `theta_meter = pi/3`, `v* = 0.9`,
  `PHI_CERT`, `PHI_LOCK_GRID` (8 uniform points), phase-locked
  conditional X-visibility figure of merit, `M = 0` conditioning,
  `vis_A = 4 sqrt(3)/7 = 0.989743`, `FLATNESS_TOL = 1e-12`,
  `ANALYTIC_TOL = 1e-9`, `IDENTITY_TOL = 1e-10`, T410's collision and
  ledger machinery.
- Fixture: `m in {1, 2, 3}`; `beta in {inf, 1.0, 0.0}` (both corners
  exact); broadcast `theta = pi`; cascade primary = resonant full SWAP,
  contrast = `controlled-Ry(pi)` (E1 control; the fallback path
  bound/undetermined predeclared); reaches `R` / `R+` / `R++` under the
  departure convention, declared once; menu = all CPTP channels on the
  reach + unlimited work ancillas (pure or Gibbs), no co-present-
  register exclusion.
- Intervention family fixed in the spec: 12 unitaries, 2 measure-then-
  act instruments, 1 sequential composition, 15 Haar samples (seed
  `20260704`, illustrative), phases `{0, 1.0, 2 pi/3}`; the LEMMA
  carries the ALL quantifier, sampling never does.
- Hand-derived corner values fixed pre-run: `TD(beta = 0) = vis_A/2 =
  0.494872`; `TD(beta = inf, SWAP) = (3 + sqrt(57))/14 = 0.753560`;
  carrier-marginal trace `(3/7) tanh(beta/2)`; `I_A = 2 h2(3/7)` /
  `h2(3/7)` bits at `beta = inf` / `0`; `Q_casc = (3/7) tanh(beta/2)`;
  `Sigma_casc(m = 1, beta = 0) = H(3/7) = 0.682908` nats; un-write work
  = T410's refund law; A's `R+` record trace `> 0.05`.
- Failure legs as reportable verdicts: `equality_fails` (halting),
  `separation_fails_A`, `separation_not_certified_B`,
  `positive_control_fails` (halting), `marginal_blindness_fails`,
  `separator_factors_through_declared_field`,
  `boundary_physicality_reduces_to_declaration` (the honest leg:
  "separation exists but boundary physicality reduces to declaration —
  absorbed"); the re-scope clause verbatim in the spec. No assertion
  weakening.
- Runtime budgets `< 300 s` (model) and `< 300 s` (suite); values at or
  below `1e-13` reported as machine floor, never as bolded run digits.

## Leg 1 — equality, observational

`rho_R^A = rho_R^B` exactly: worst entrywise difference over both
phase grids x 3 `m` x 3 `beta` x 2 cascades (252 comparisons) at
machine floor (< 1e-13; tol 1e-12). Spot value: `rho_R = diag(4/7,
3/7)` on `{|00>, |11>}`, phi-independent exactly at every `beta` (the
`theta = pi` broadcast multiplies the coherence block by
`f^m = cos(pi/2)^m = 0`).

## Leg 2 — equality, interventional (lemma-carried)

The environment-side channel lemma carries the ALL quantifier; the
numerics assert it at machine floor everywhere (54 (state-pair, phase)
cells; tol 1e-12):

| Assertion | Worst over the sweep |
| --- | --- |
| operator-level: abstract-Kraus `Lambda_E`(A) = B (both cascades; no tier-2 register appended) | machine floor |
| 12 unitary interventions: post `R` marginals | machine floor |
| instruments I1/I2: branch probabilities / post states | machine floor |
| sequential composition (I1 → Haar → I2): joint distribution / final states | machine floor |
| 15 seeded Haar rows (illustrative) | machine floor |
| commutation in action `Lambda_E(Phi_R(A)) = Phi_R(Lambda_E(A))` | 0.0 |

A later R-statistic depends on a conditioned joint state only through
its `R` marginal, so per-branch `R`-marginal equality covers arbitrary
continuations — the quantifier is the lemma's, not the family's.

## Leg 3 — forcing at `R`

Both capability states, both cascades, every `(m, beta)`: verdict
`final-relative-to-R`; phi-independence certificate and trace-norm
bound at machine floor (< 1e-13; tol 1e-12); work-dilation invariance
(pure + Gibbs registers) at machine floor. **The restoration task
cannot be re-posed inside `R` at any work expenditure, for either
state** — boundary crossing is forced by the task, and `R -> R+` is
exactly where the two states split. (Disclosed: T400's compulsion
clause — an agent FORCED to attempt — is not modeled; forcing is
task-relative and certificate-carried.)

## Leg 4 — separation at `R+`: one functional, one menu, opposite verdicts

| Cell (every `m`, every `beta`) | achieved | phi cert | verdict |
| --- | --- | --- | --- |
| **A** (un-write) | **0.989743** (= `vis_A`, < 1e-9; >= v* = 0.9) | 0.107–0.857 (record trace; min **0.107143** > 0.05 floor) | **restorable-at-R+** |
| **B, SWAP cascade** | 0.0 (same un-write; machine floor) | machine floor (< 1e-13) | **final-relative-to-R+** (certified_by = phi_independence) |
| **B, broadcast cascade** (contrast) | — | machine floor (< 1e-13) | **final-relative-to-R+** (fallback path predeclared, not needed) |

- B(SWAP)'s retained state is an exact product `rho_R (x)
  tau_beta^(x)m`: worst residual at machine floor — the strong
  certificate's premise, and the leg-7 joint-record answer.
- Dilation invariance of B's certificate and bound under adjoined work
  registers (one pure, one Gibbs): all diffs at machine floor; the
  unlimited-work claim is carried by the Stinespring lemma
  (`||A (x) sigma||_1 = ||A||_1`), numerics assert two registers
  (disclosed, T410 convention).
- A's nonzero `R+` record trace is REQUIRED by T408's menu-support
  obstruction lemma (a capability gap needs a statistical trace within
  the separating menu's support); the flat surface (`R`) is exactly the
  complement of that support. Min certificate value 0.107143 at
  `(beta = 0, m = 3)` — the hand-derived `ab sqrt(3)/2^m` corner.

## Leg 5 — positive control at `R++`, priced

**B is `restorable-at-R++` at every `(m, beta)`, both cascades:**
achieved `= vis_A` to machine floor (un-cascade + un-write; A stays
restorable with fresh uncollided tier-2 appended). The relocation is
priced (T410 ledger conventions; bookkeeping, not a theorem):

| `beta` | `W_casc` (SWAP) | `Q_casc` per carrier | `Sigma_casc` (m = 1, nats) |
| --- | --- | --- | --- |
| inf | 0.0 | **0.428571** (= 3/7) | inf (typed: zero-temperature limit — disclosed, not asserted) |
| 1.0 | 0.0 | 0.198050 | 0.880958 |
| 0.0 | 0.0 | 0.0 | **0.682908** (= `H(3/7)` = the departed mutual information; residual < 1e-13) |

- `Q_casc = (3/7) tanh(beta/2)` closed form: residual at machine floor.
  Sigma two-way identity (`Delta S + beta Q = I + D`) at machine floor
  for every finite-`beta` cascade; `min Sigma = 0.0 >= -1e-10`.
- **The `beta = 0` corner, predeclared as expected and NOT spun as
  entropy pricing:** the cascade moves the record out with ZERO work
  and ZERO heat, yet books `Sigma = H(3/7) = 0.682908` nats — exactly
  the mutual information the departing tier carries away (the
  identity's `I` term with `D = 0`). The ledger registers the record's
  departure even when no energy moves. (`Sigma_casc(beta = 1)` also
  reproduces T410's per-collision `Sigma` at `theta = pi, beta = 1` —
  0.880958 — as it must: same identity, same machinery.)
- Restoration work at `R++`: un-SWAP work at machine floor (SWAP
  commutes with the resonant free Hamiltonian); un-write work equals
  T410's refund law `-m w_fwd(pi, beta) - 3/7` cross-module (residual
  < 1e-13).
- **Honest scope, inherited verbatim from T410:** departure-by-trace is
  the standard reservoir idealization — adopted, not derived; boundary
  physicality is conditional on the model class; the transport rung
  stays named-unbuilt.

## Leg 6 — `beta = 0` marginal blindness (the sharpest tooth)

- **All 200 enumerated proper-subset marginals of the `R+` register set
  are identical between A and B at `beta = 0`** (every single register,
  every pair, every subset short of the full joint; both cascades; diff
  exactly 0.0): "which registers hold the discriminating datum" has no
  answer short of everything.
- The FULL joints differ — the datum exists, jointly: trace distance
  exactly `vis_A/2` = **0.494872** at every `m` (< 1e-9 vs the
  hand-derived value). At `beta = inf` (SWAP) TD = **0.753560**
  (`= (3 + sqrt(57))/14`, every `m`); broadcast-cascade TD at
  `beta = inf` is 0.494872 (reported).
- The LOCAL trace of dispersal depth fades as `(3/7) tanh(beta/2)`:

  | `beta` | inf | 1.0 | 0.0 |
  | --- | --- | --- | --- |
  | carrier marginal diff A vs B | **0.428571** (= 3/7) | 0.198050 | **0.0** |

  while both equality certificates (legs 1–2) and both capability
  verdicts (legs 3–4) are `beta`-blind.
- At `beta = 0` the two cascade variants produce IDENTICAL retained
  states (diff at machine floor; both `rho_R (x) (I/2)^(x)m`): moving
  the record out and copying it deeper leave the same zero local trace.
- Mutual information `I(R : E1)` (bits, `m = 1` row; closed forms
  < 1e-9):

  | `beta` | `I_A` | `I_B` (SWAP) | `I_B` (broadcast) |
  | --- | --- | --- | --- |
  | inf | **1.970457** (= 2 h2(3/7)) | 0.0 | **0.985228** (= h2(3/7), the classical which-way record — retained, and still final) |
  | 1.0 | 1.142141 | 0.0 | 0.156912 (= T410's escaped Holevo `chi_1(beta = 1)`, as it must) |
  | 0.0 | **0.985228** (= h2(3/7)) | 0.0 | 0.0 |

  (Flagged, from memory, unverified: no-hiding-theorem lineage,
  Braunstein–Pati; discord-type locally-hidden correlations.)

## Leg 7 — absorber-control battery

- **T399 (enlarged-state completion):** the positive control IS the leg
  — enlargement restores and is priced (leg 5). At FIXED `R+`, with
  full access to everything the B run retains, the split is sourced in
  dispersal depth whose B-side trace is exactly zero. B is final
  relative to EVERYTHING retained in its run; the `R++` run is a
  different, counterfactual retention, run as the priced control.
- **T400 (hidden datum / stipulated label):** two copies of the same
  physical preparation with different stipulated flags are
  bit-identical (diff == 0.0) with identical verdicts — a stipulated
  label alone moves nothing, while in T411 every declared field is
  matched and the capability still splits. Compulsion clause disclosed
  as not modeled.
- **T401 (joint-record completion):** for B(SWAP) the complete retained
  joint state is an exact product (`I(R : E1) = 0.0`) — the completion
  is exhausted and returns nothing; completing further requires the
  departed tier, i.e. the open-system premise itself. Sharper, via the
  broadcast contrast: there the joint-record completion DOES return the
  which-way datum (classical `h2(3/7)` bits at `beta = inf`) and the
  verdict is STILL `final-relative-to-R+`, certified — record datum and
  capability are transverse.
- **T402/T403/T405/T406 (matched completions):** declared-fields dicts
  equal between A and B at every reach (asserted); the only differing
  item is the physical cascade history, which carries an energy/entropy
  ledger entry (leg 5), not a bookkeeping entry; the verdict functional
  is one label-free code path and swaps with its input (asserted) — no
  flag, latch field, or transition relation is consulted.
- **T398/T404 (resource projection, run on itself):** the per-reach
  profiles `C(A) = (final@R, restorable@R+)`,
  `C(B) = (final@R, final@R+, restorable@R++)` absorb as order content
  exactly as T398 showed — **conceded**. Residues at the frame's
  interface, both inheriting T404's predeclared conditional demotion
  clause: (a) causal indexing — work, the frame's own currency,
  certified unable to purchase what reach purchases (candidate
  absorbers: dynamical resource theories / superchannels — named, from
  memory, unverified); (b) the `beta = 0` locally-hidden sharpening
  (candidate absorber: Blackwell comparison of experiments — named,
  from memory, unverified).
- **T397 / SBS closure key:** no class markers, no closure key; the
  task is a threshold on a computed functional, not a label readout.
- **Lieb-Robinson:** no geometry (all-to-one contact graph); a
  departure boundary, **not a light cone** (named from memory,
  unverified).
- The spec's re-scope clause stands ready if review shows even the
  assembly is standard (environment-assisted recovery / quantum
  Darwinism / data processing on a dilation).

## Guardrails

- **Q1D (with teeth):** declared `(S, M)` record (unconditioned
  Z-distribution) invariant across variant, `m`, `beta`, cascade
  (machine floor; broadcasts and cascades never touch `S` or `M`);
  every R-supported intervention in the leg-2 family moves every
  tier-1 marginal by < 1e-13 (no signalling out of `R`); the
  counterfactual `R+` un-write moves them by exactly
  `(3/7) tanh(beta/2)`: **0.428571** at `beta = inf` (the house tooth),
  0.198050 at `beta = 1`, 0.0 at `beta = 0` — where the tooth is
  instead the restored-visibility jump (at `R`: 0.0; at `R+`:
  0.989743), leg 6's marginal blindness made explicit.
- **R1:** untouched. A departure boundary through an interaction
  sequence, not a light cone (Lieb-Robinson named as an absorber risk,
  from memory, unverified).

## Tests

`tests/test_departed_record_capability_discriminator.py`: **42 passed**
(~8 s). Neighbor suites re-run green alongside in one invocation:
T410 (48), T409 (36), T408 (35), T393 (29), T392 (18), T142 (6) —
**214 passed** total, 45.9 s wall. Model run ~6–7 s. Predeclared
budgets < 300 s each: met with wide margin; no fallback needed.
Deterministic; exact density-matrix evolution on T410's machinery,
cross-pinned to T410's `prepare_retained` at the A-preparation anchors
(machine floor); the only sampling (Haar, seed `20260704`) is
illustrative. "Exhaustive" is used only for the literal proper-subset
enumerations of leg 6 (200 subset marginals) — everything else is
finite-witness at the predeclared sweep. One in-flight correction
before any results were recorded: the model's verdict sentence was
edited from "review decision. No claim promotion" to "review decision;
no claim promotion" to carry the house-required lowercase substring —
a wording-only change, no computed quantity or leg logic touched (the
suite's substring assertion caught it; 41/42 green before, 42/42
after).
Reproduction:

```bash
python -m pytest tests/test_departed_record_capability_discriminator.py -v
python -m models.departed_record_capability_discriminator
```

## What this does not earn

No unconditional physical-boundary claim: departure-by-trace is the
standard reservoir idealization, adopted, not derived — a hostile
reviewer may reject the model class and relocate the boundary, and the
verdict language concedes this (T407's declared-vs-physical standing
objection is NOT claimed discharged unconditionally; the transport
rung — work literally buying reach — stays **named-unbuilt**). No
resolution of the open problem by itself: the fixture realizes the
success criterion at its finite, region-relative, model-class-
conditional level; whether that discharges the discriminator burden is
a hostile-review and promotion decision that pauses for Joe. Two leg-7
absorptions are CONCEDED (T398-style order content; T399-style
enlargement, priced), and both claimed residues carry T404's
conditional demotion clauses with named, unverified candidate
absorbers. The T400 compulsion clause is not modeled. No thermodynamic
theorem: the leg-5 ledger is exact-identity-backed bookkeeping at the
computed states of a finite model (finite-witness per
COMPLEXITY-LEDGER vocabulary). No new mathematics or physics: every
assembled fact is presumed standard (environment-assisted recovery,
hazy-environment Darwinism, no-hiding lineage, EGY, Blackwell — all
flagged from memory, unverified); the claimed residue is the
assembled, certificate-disciplined object, and the spec's re-scope
clause stands ready. No hardware or platform. No CLAIM-LEDGER
movement; TESTS.md untouched (new T-number availability noted in the
header of this file only). Hostile review QUEUED, not survived. All
promotion decisions pause for Joe.

---

## Addendum (2026-07-02): adapter-test gauntlet — discriminator ABSORBED

The four-absorber adapter gauntlet was run against this artifact's actual
model (evidence scripts in `_local/`, gitignored scratch). Run record:
`steward/runs/2026-07-02-physical-boundary-swing.md`. This addendum
relabels; the original text above is preserved unedited as the historical
record. The verdict "the departed-record capability discriminator holds"
stands ONLY at the finite-witness computed level (the legs reproduce); its
promotion to a **physical-boundary** discriminator — the swing's actual
target — is **ABSORBED**. The re-scope clause invoked below was already
carried in this file as the fallback standing.

**Gauntlet verdict: ABSORBED. Killing move: joint-record completion (the
T401 move), corroborated by the decoherence-bookkeeping absorber.**

1. **Resource-theory absorber (T398) — partially-absorbed.** T411 relabels
   as U(1)-asymmetry / athermality resource theory under a nested reach
   filtration: both states free at R, A activates at R+, B free at R+, a
   total chain `B =< A`. Both CLAIMED residues absorb — "work cannot buy
   reach" is the free-ancilla monotonicity axiom (the artifact's own
   dilation lemma), and `beta = 0` non-locality is ordinary global-monotone
   marginal blindness. Sole survivor: the assertion that the R -> R+ cut is
   physically forced — outside resource theory's vocabulary, and exactly
   what leg 5 concedes is adopted, not derived.

2. **Joint-record completion (T401) — ABSORBED (the kill).** State A and
   state B both RETAIN their tier-1 carriers (only tier-2 departs), so in
   both runs tier-1 is physically CO-PRESENT. R is a declared strict subset
   of the retained register set; excluding retained tier-1 from R is pure
   declaration. Computed against this model: in A the retained tier-1
   carries `I(R:E1) = 1.970456` bits at `beta = inf`, and the leg-1/2
   R-equality is literally the R+ state's marginal after discarding this
   retained correlated register (R-marginal == R-run to 0.0); a single
   Z-measurement on the retained `E1_1` separates A from B
   (`P = 0.428571` vs `0.0`); at `beta = 0` the full retained R+ joint
   distinguishes A from B at TD = 0.494872. The separating boundary
   R -> R+ crosses only declared bookkeeping. The one genuinely physical
   departure (tier-2, R+ -> R++) does no discriminating work — A and B
   already differ at R+ (TD = 0.753560) before any tier-2 access — and
   drives only leg 5's priced enlargement control, which this file already
   concedes is T399-standard. This is the enlarged-state / joint-record
   completion the open problem's Failure/Demotion criteria forbid.
   **Correction to leg 7's T401 entry:** the entry answers only the R+
   completion (B's retained joint is a product, `I(R:E1) = 0`) and misses
   that the EQUALITY region R is itself a declared subset of the co-present
   retained set — which is where the absorber strikes. The honest failure
   leg `boundary_physicality_reduces_to_declaration` did not fire because
   it audits only whether SEPARATION verdicts at R+ are certificate-carried
   on the retained state (they are); it has a blind spot for the provenance
   of the EQUALITY region R.

3. **Decoherence-bookkeeping absorber — ABSORBED.** Every load-bearing
   object reduces to standard open-system decoherence plus standard
   thermodynamic/information accounting: `f = cos(theta/2)` the mixed-state
   decoherence factor; leg-5 `Sigma = dS + beta Q = I + D` the
   Esposito-Lindenberg-Van den Broeck identity (`|Sigma - (I+D)|` worst
   7.8e-16); A-vs-B the generic complementary-channel (eraser-accessible vs
   escaped) recoverability dichotomy; `r(n) = n` quantum-Darwinism
   redundancy. The discriminating boundary is a hand-imposed partial trace
   keyed on a stipulated "retained" set (`prepare_retained`) — the
   reservoir idealization, **declared, not dynamically forced.** At the
   model-class level this FIRES `boundary_physicality_reduces_to_declaration`.

4. **Lieb-Robinson relabel — survives (this content is genuinely not a
   light cone).** No support/light-cone functional separates A from B (the
   discriminating datum is a global correlation in no proper subset: at R+,
   `beta = 0`, worst proper-subset diff 0.0 while full-joint diff 0.124);
   the verdict flip (B-at-R++ restorable vs B-at-R+ final under an
   identical forward unitary) is the partial-trace departure step, a
   data-processing boundary LR is blind to; the all-to-one contact graph
   makes an LR light cone degenerate. This defends the guardrail claim
   ("a departure boundary, not a light cone") but is orthogonal to the
   kill — surviving the wrong absorber does not rescue the physical
   boundary.

**Net standing (relabel).** The physical-boundary discriminator claim is
absorbed: the separating boundary is declared bookkeeping (joint-record
completion), and the one physical boundary is a reservoir-idealization
partial trace that does not carry the discrimination. T407's
declared-vs-physical objection stands undischarged — consistent with this
file's own conditional "adopted, not derived" language. The surviving
content is the re-scoped standing this file already carried: a
repo-internal calibration of the certificate toolkit (all-channel
certificates, graded bands, T142 ledger) on a thermal quantum-Darwinism
collision family, with the priced enlargement control and the
Lieb-Robinson-irreducibility fact as residue. The primary open problem
(`open-problems/region-indexed-capability-discriminator.md`) remains OPEN;
the physical-escape mechanism is the still-named-unbuilt transport rung.
No claim promotion; no CLAIM-LEDGER / TESTS.md edits; ledger actions pause
for Joe.
