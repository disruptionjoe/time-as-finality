# T384 Results: Relativity Substrate Synthesis Packet

## Current Strongest Claim

Given:

- a generated shared compatibility substrate,
- exactly two independent primitive null compatibility-signal directions,
- a symmetric bilinear interval,
- reciprocal round-trip observer calibration,
- invariant signal-unit normalization,
- exact access and minimality,

the finite screen forces Lorentz-pattern observer relativity:

- observer simultaneity disagreement,
- gamma/beta Lorentz-pattern coefficients,
- time dilation from round-trip calibration,
- reciprocal interval-preserving observer transforms,
- shared interval recovery without source time/space columns.

This is a conditional pattern theorem, not a physical derivation of relativity.

## Ladder Findings

| test | finding |
|---|---|
| T377 | Fixed no-time-column compatibility table can render observer-relative simultaneity with a shared interval. |
| T378 | Rank-like structure can be generated from local compatibility edges rather than stored as source columns. |
| T379 | Given invariant signal propagation and round-trip calibration, Lorentz-pattern beta/gamma/time dilation are derived. |
| T380 | Two independent null channels force the product interval up to scale, but the two-channel basis is not derived. |
| T381 | Within declared observer-relativity requirements, the minimal two-null-channel adapter is the unique survivor. |
| T382 | Among declared external adapter shapes, the two-null-channel adapter is uniquely clean. |
| T383 | Targeted perturbations show the pattern is premise-sensitive but fails in interpretable ways. |

## Exact Premise Bundle

| premise | statement |
|---|---|
| `shared_generated_substrate` | Shared compatibility substrate exists; source rows do not store observer time/space coordinates. |
| `two_independent_null_channels` | Adapter supplies exactly two independent primitive null compatibility-signal directions. |
| `bilinear_interval` | Interval object is a symmetric bilinear form over two signal lineages. |
| `round_trip_calibration` | Observers calibrate by reciprocal round-trip signal counts. |
| `invariant_signal_unit` | Primitive signal unit is shared; `c = 1` is a rest-rendering normalization. |
| `exact_access_minimality` | Adapter has exact record-level access and no extra primitive signal direction. |

## Open Objects

| object | open question |
|---|---|
| `basis_origin` | Why exactly two primitive null compatibility-signal directions? |
| `bilinear_null_premise` | Why symmetric bilinear interval and null primitive signals? |
| `nonfinite_nonfixed_extension` | Can this move beyond finite generated closures and fixed catalog screens? |
| `physical_interpretation` | Is the two-null-channel adapter internal substrate structure or an external interface? |

## Plain-English Synthesis

The campaign moved the question from:

```text
Does this look relativistic?
```

to:

```text
What exact adapter makes relativity unavoidable?
```

The answer is conditional:

```text
If the shared substrate has a minimal two-null-channel signal interface, the
Lorentz pattern follows.
```

The remaining hard problem is:

```text
Why does that two-null-channel interface exist?
```

It might be internally derived later. It might be independently motivated. Or it
might be the shape of the external adapter.

## Claim Ledger Update

Register T384 as the campaign synthesis:

```text
conditional_lorentz_pattern_forced_open_object_is_two_null_basis_origin
```

## Next Goal

Attack `basis_origin`:

```text
derive, motivate, or falsify the existence of exactly two primitive null
compatibility-signal directions.
```
