# T390: Class-Shape Alignment Screen

## Target Claims

D1, R1, S1, PO1, T389, and the GU-formalization class material:

```text
Does TaF's record-finality-to-two-leg structure fit the broader class geometry
described in the GU formalization repo, without forcing a GU-specific match?
```

## Setup

Use the GU-formalization repo as read-only class-level source material:

- `RESEARCH-PROGRAM.md`,
- `RESEARCH-POSTURE.md`,
- `docs/WHERE-GU-STANDS-AND-THE-MISSING-OBJECT-2026-06-27.md`,
- `canon/no-go-class-relative-map.md`.

Compare TaF objects against class-level features:

- observer universe / external boundary data,
- missing source-action rulebook,
- class-level external chirality law,
- forgetful-image relation-side pattern,
- Riemannian/Ehresmannian shadow pattern.

## Success Criteria

- Use class-level material, not GU-specific vindication.
- Identify natural fits.
- Reject overfit mappings.
- Preserve the two-leg-to-null bridge as open unless actually derived.
- Produce a concrete next screen.

## Failure Criteria

- Treat GU as solved or as the target.
- Map TaF's two signal directions to generation count or chirality.
- Revive the killed "source action = observer slice" analogy.
- Identify 14D class geometry directly with TaF's two protocol legs.
- Claim nullness or bilinearity from class material without a bridge.

## Known Constraints

The GU-formalization worktree may contain unrelated dirty changes; this screen
uses its documents as read-only context and records the extracted class facts in
TaF.

## Contribution Needed

If the class material supplies a good hypothesis but not a derivation, the next
goal should test that hypothesis directly.
