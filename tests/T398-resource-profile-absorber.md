# T398: Resource-Profile Absorber for C(R)

## Route

Direction A / bounded-region capability object / absorber audit.

This is the named next gate after
[T397: Region-Indexed Capability No-Go](T397-region-capability-no-go.md).
T397 built the first finite `C(R)` profile object and explicitly priced the
resource-theory absorber before any promotion. T398 runs that gate internally.

## Claim

In the finite T397 family, once the declared capability profile `C(R)` is
admitted as the resource state and the declared region/menu/task tuple is
admitted as the resource-theory context, the formal anti-scalar content is
absorbed:

```text
resource object = mutual-convertibility class of C(R) profiles
resource preorder = componentwise capability dominance
```

The resulting quotient is exactly the product of a 3-chain of undo capability
and a 4-chain of readout capability. Incomparability and scalar failure are
therefore ordinary non-total resource-preorder facts in this finite audit, not
new resource-theory residue.

The T397 statistics-flat class is likewise absorbed as an incomplete observer
shadow: declared Z-readout statistics omit the admitted resource profile. Once
the profile is granted, every capability verdict factors through it.

## Class

Absorber audit. No claim promotion. No `CLAIM-LEDGER.md` movement.

This artifact does **not** verify external LOCC, coherence, majorization, or
general resource-theory literature. It runs the finite internal absorber gate
that T397 named. Verified external prior art remains a separate literature
task before any external-facing resource-theory language.

## Status

Implemented. The absorber is granted at the finite resource-preorder level.
`C(R)` remains useful as a region-indexed audit/readout object and physical
realization, but not as a promoted resource-theory novelty claim.

## Target Claims

- T397 `C(R)` capability object (exploratory)
- [T394: Axis-Count Reconstruction Hierarchy](T394-axis-count-reconstruction-hierarchy.md)
- [T395: Record-Order Trade-off Probe](T395-record-order-tradeoff-probe.md)
- [Region-Indexed Capability Discriminator](../open-problems/region-indexed-capability-discriminator.md)

## Setup

Reuse T397 unchanged:

- 24 configurations;
- 12 distinct `C(R)` profiles;
- four task coordinates:
  `undo_within`, `undo_cross`, `order_postdiction`, `class_readout`;
- T397's componentwise enactability order;
- T397's statistics-flat 16-configuration class.

Build the quotient by identical `C(R)` profile. Declare resource order:

```text
a <= b iff b dominates a componentwise
```

Interpretation: a higher profile can simulate a lower one by declining or
discarding available task capability.

## Success Criteria

- The resource quotient has 12 objects and accounts for all 24 configurations.
- Its preorder has T397's shape: 48 reflexive-or-comparable pairs and 18
  incomparable pairs.
- Two axes jointly classify it: undo rank and readout rank.
- Every capability verdict factors through the admitted resource profile.
- T397's 16-member statistics-flat class spans all 12 resource objects,
  proving the declared statistics projection is resource-underdescribed.
- Absorber verdict records translation residue only.

## Failure Criteria

The absorber would fail if:

- two same-resource-profile configurations split on a capability verdict;
- the admitted resource quotient failed to reconstruct the T397 preorder;
- the statistics-flat class no longer spanned multiple resource profiles;
- a same-context region-indexed obstruction survived beyond the admitted
  resource profile and context.

## Known Physics Constraints

Inherited from T397. T398 adds no hardware, platform, spacetime, quantum
interpretation, or physics-facing claim.

## Contribution Needed

- Stronger region-indexed discriminator: equality under all `R`-supported
  intervention statistics, with separation only by a boundary-crossing menu.
- Partial-amplitude deformation of the T397 profile grid.
- Verified literature note before external-facing resource-theory phrasing.
