# T493: Levin Competency C(R) Absorber Gate

## Target Claims

- `C(R)` capability object.
- T407 region-indexed capability no-go.
- T398 resource-profile absorber.
- N16 Levin/Fields competency calibration neighbor.
- `open-problems/region-indexed-capability-discriminator.md`.

## Setup

Use the existing T407 finite `C(R)` family and T398 resource-profile absorber
as the internal executable substrate. Treat N16 as pointer-grade literature
context, not as primary-source-checked authority.

Audit three readings:

1. Competency as the full intervention-measured task-success profile over a
   declared region, menu, and task family.
2. Competency weakened to one observed navigation or readout success statistic.
3. T407's zero-trace statistics/capability split as a possible surplus target
   over simple observed statistics.

## Success Criteria

- Full-profile competency absorbs current T407/T398 `C(R)` without claim
  movement.
- Single-goal observed-statistic readings fail against the multi-task `C(R)`
  profile.
- T407 zero-trace flatness remains only a review target over simple observed
  statistics, not a novelty claim over full competency.
- External mechanism import, public posture, and cross-repo truth shortcuts are
  blocked.

## Failure Criteria

- The gate treats N16 resonance as evidence for novelty.
- The gate imports active-inference or free-energy mechanism language into TaF.
- The gate treats a single observed statistic as equivalent to the full `C(R)`
  profile.
- The gate updates claim status, public posture, or cross-repo absorber truth.

## Known Physics Constraints

No physics claim is made. The artifact is a finite internal audit over existing
TaF executable objects and pointer-grade literature context.

## Contribution Needed

Run:

```text
python -m models.levin_competency_cr_absorber_gate --write-results
python -m pytest tests/test_levin_competency_cr_absorber_gate.py -q
```

Before any external-facing use of N16, verify the primary sources in a separate
literature absorber note.
