# T497: Bounded Retrieval Source-Checked Stack Gate

## Target Claims

T495 bounded-record retrieval bottleneck; N18 bounded retrieval/copying source
note; composite absorber-stack progress lanes; no claim-ledger target.

## Setup

Use the Bridge-Functor admission packet framing on the highest-priority
composite progress lane:

```text
observer budget
+ retained-state summary
+ retrieval workload
+ full-history absorber
+ source-checked lower-bound literature
```

The executable fixture generalizes T495's finite stream check over binary
streams of length 2 through 8:

- full history vs arbitrary indexed retrieval;
- last-2 committed state vs arbitrary indexed retrieval;
- last-2 committed state vs retained-suffix retrieval.

The source check is recorded in N18 and uses primary literature only for the
narrow computation-side copying/retrieval workload.

## Success Criteria

- N18 source status is recorded for bounded copying/retrieval workload language.
- Full history factors arbitrary indexed retrieval for all tested lengths.
- Last-2 state has non-singleton arbitrary-retrieval spread for lengths greater
  than 2, with spread `2^(n-2)`.
- Last-2 state factors its native retained-suffix task for all tested lengths.
- The admitted result is composite absorber explanation/review target only.
- Complexity-theorem import, physics-mechanism import, quantum-copyability
  revival, public-posture movement, and cross-repo shortcuts are rejected or
  blocked.

## Failure Criteria

- T497 is used as a proof of an SSM/Transformer theorem rather than a
  source-checked finite review target.
- T497 is used as physics mechanism, decoherence, Standard Model, quantum
  copyability, claim-ledger, public-posture, or cross-repo evidence.
- Bounded summaries fail their native retained-task controls.
- Source caveats about selective SSMs, fast-weight memory, and architecture
  scope are erased.

## Known Physics Constraints

This is not a physics result. It does not assert that physics is a Transformer
or SSM, does not prove quantum copyability, and does not derive finality as a
physical mechanism.

## Contribution Needed

If this lane continues, build a theorem-facing packet that restates one checked
copying/retrieval lower-bound theorem with its exact assumptions and compares
that theorem's state model to the finite T497 capability-spread fixture.
