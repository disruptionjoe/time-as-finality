# T544: APRD Minimality And Absorber Separation Gate

## Target Claims

- S1 and TAF11 only as a source-law feeder branch.
- TAF8 only as a possible future shadow-protection feeder.
- No claim-ledger, Canon Index, canon-verdict, public-posture, North Star,
  external-publication, or cross-repo movement.

## Purpose

T543 built APRD, accessible provenance reconstruction debt, as a set-valued
boundary object. T544 tests whether APRD clears the next gate:

- fixed before outcome inspection;
- set-valued rather than scalar rank;
- tested against same-neighbor-data completion;
- absorbed when a native absorber legitimately supplies the missing sections;
- surviving on at least one non-detector record/provenance fixture;
- minimal, with every surviving debt atom load-bearing.

## Success Criteria

1. T544 consumes the T543 APRD object and verdict.
2. The T66 detector threshold/provenance control is absorbed by native
   completion.
3. At least one non-detector APRD fixture survives same-neighbor-data
   completion.
4. Survivor APRD sets are minimal: padded debt rejects.
5. APRD does not collapse to scalar rank: same-rank survivor debt sets remain
   distinct, and scalar-only replacement rejects.
6. Post-hoc outcome-selected debt rejects.
7. Full access produces no APRD.
8. No claim, canon, public-posture, North Star, external-publication, or
   cross-repo movement is made.

## Expected Result

`aprd_minimality_gate_finds_nonabsorbed_provenance_survivor`.

T544 should keep APRD alive as a finite source-law feeder, not a source law.
The next useful packet should test refinement, relabeling, and restriction-map
stability before any theorem reading.

## Reproduction

```bash
python -m models.t544_aprd_minimality_absorber_separation_gate --write-results
python -m unittest tests.test_t544_aprd_minimality_absorber_separation_gate -v
```
