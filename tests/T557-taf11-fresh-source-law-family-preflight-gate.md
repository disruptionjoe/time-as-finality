# T557: TAF11 Fresh Source-Law Family Preflight Gate

## Target Claims

S1, TAF11, TAF4, TAF8, T556, T548, T541.

## Setup

Consume T556 as route-reset authority:

- the T550-T555 Observerse protocol-stack route is parked as audit translation
  residue only;
- more bounded Observerse fixtures are rejected as source-law evidence;
- renewed TAF11 movement requires a fresh source-law family with source
  variables, mature absorbers, and falsifiers declared before target reading;
- TAF4, TAF8, claim-ledger, Canon Index, public-posture, publication, and
  cross-repo shortcuts remain blocked.

Evaluate possible fresh-family preflight routes:

- select a fresh TAF11 family and next executable packet;
- reject Observerse and APRD replay as fresh-family substitutes;
- reject target-label or cross-repo imports;
- reject moving TAF4, executing TAF8, or changing claim/canon/public posture
  from preflight selection alone.

## Success Criteria

- T556 is consumed as the source state.
- Exactly one fresh family is selected.
- The selected family declares source variables, mature absorbers, and
  falsifiers before target reading.
- The selected family names exactly one next executable packet.
- Observerse protocol-stack replay and APRD replay are rejected.
- Target-label and cross-repo imports are blocked.
- TAF4 finite-to-continuum movement and TAF8 cross-domain execution are blocked.
- No claim-ledger, Canon Index, canon-verdict, public-posture, North Star,
  external-publication, or cross-repo movement is made.

## Failure Criteria

T557 fails if:

- T556 is not the source authority;
- the absorbed Observerse route is reused as source-law evidence;
- APRD replay is treated as a fresh family after T548 narrowing;
- a family is selected without source variables, mature absorbers, or
  falsifiers;
- the selection reads target labels or cross-repo truth;
- TAF4, TAF8, external publication, or cross-repo truth is moved.

## Known Physics Constraints

No physics, geometry, causal-set, Lorentzian, quantum, source-law, or
shadow-protection claim is earned by this gate.

## Implementation Result

Status: implemented.

T557 consumes T556 as route-reset authority and selects
`sheaf_obstruction_transport_family` as the fresh TAF11 family preflight. The
family declares finite event covers, local finality sections, restriction
morphisms, settlement obstruction witnesses, transport consistency squares, and
allowed refinement steps as source variables. It predeclares sheaf gluing,
resource transport, consensus state-machine, and provenance-completion
absorbers plus four falsifiers.

T557 selects `t558_sheaf_obstruction_transport_source_law_packet` as the next
TAF11 burden. That next packet may test the family; T557 itself only declares
the preflight contract.

## Run Command

```bash
python -m models.t557_taf11_fresh_source_law_family_preflight_gate --write-results
python -m unittest tests.test_t557_taf11_fresh_source_law_family_preflight_gate -v
```

## Boundary

T557 is a fresh-family preflight. It does not establish a source law, move TAF4,
execute TAF8, or change claim/canon/public posture.
