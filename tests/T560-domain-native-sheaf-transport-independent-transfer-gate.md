# T560: Domain-Native Sheaf Transport Independent Transfer Gate

## Target Claims

S1, TAF11, TAF4, TAF8, T559, T558, T557, T556, T548, T541.

## Setup

Consume T559 as the direct transfer authority. The frozen family remains
`sheaf_obstruction_transport_family`, with these source variables:

- finite event cover;
- local finality sections;
- restriction morphisms;
- settlement obstruction witness;
- transport consistency square;
- allowed refinement steps.

The mature absorbers remain ordinary sheaf gluing completion, resource
transport monotone, consensus state-machine, and record-provenance completion.
The falsifiers remain gluing under declared restrictions, transport-square
commutation after mature absorbers, relabel-only target realization, and hidden
target/cross-repo import.

## Success Criteria

- T559 is consumed as independent-transfer authority.
- The T557/T558/T559 contract remains frozen.
- The T559 admitted payload remains frozen as
  `record_finality_transport_square_payload`.
- A T559 payload replay is rejected as self-confirming rather than independent.
- Every mature absorber receives a transfer control case.
- Every declared falsifier is triggered by at least one control case.
- Observerse replay, APRD replay, target import, relabel-only transfer,
  formal-only residue, TAF4 movement, and TAF8 execution reject.
- Exactly one independently shaped finality-native transfer payload is admitted
  as review material.
- The admitted transfer is record-disjoint, multi-phase, partitioned differently
  from T559, and keeps a noncommuting transport square after the four mature
  absorbers.
- No source law, claim-ledger, Canon Index, canon verdict, public posture, North
  Star, external publication, TAF4 movement, TAF8 execution, or cross-repo
  movement is made.

## Failure Criteria

T560 fails if:

- the frozen T557/T558/T559 contract changes;
- the T559 payload is not treated as frozen source state;
- a T559 replay is admitted as independent transfer;
- a mature absorber is skipped;
- a declared falsifier is untested;
- a spent route, target import, cross-repo import, or relabel-only shortcut is
  admitted;
- a formal-only residue is treated as domain-native;
- independent transfer is treated as source-law evidence;
- TAF4, TAF8, external publication, or cross-repo truth is moved.

## Known Physics Constraints

No physics, geometry, causal-set, Lorentzian, quantum, source-law, or
shadow-protection claim is earned by this packet.

## Implementation Result

Status: implemented.

T560 consumes T559 as transfer authority and admits one independently shaped
domain-native handoff/rotation/repair transfer payload as review material. The
admitted transfer carries the frozen source variables, uses operation menus
derived from local finality sections and allowed refinements, is record-disjoint
and differently partitioned from the T559 payload, and keeps a noncommuting
transport square after ordinary sheaf gluing, resource transport,
consensus/state-machine, and provenance-completion absorbers.

This does not establish a source law. The next burden is
`t561_domain_native_sheaf_transport_generalization_boundary_gate`: treat T559
and T560 as two bounded domain-native survivors, then test the generalization
boundary before any source-law reading.

## Run Command

```bash
python -m models.t560_domain_native_sheaf_transport_independent_transfer_gate --write-results
python -m unittest tests.test_t560_domain_native_sheaf_transport_independent_transfer_gate -v
```

## Boundary

T560 is an independent-transfer gate. It can justify a
generalization-boundary gate, but it does not supply evidence strong enough for
claim-ledger, Canon Index, source-law, public-posture, TAF4, or TAF8 movement.
