# T562: Domain-Native Sheaf Transport Minimality Gate

## Target Claims

S1, TAF11, TAF4, TAF8, T561, T560, T559, T558, T557, T556, T548, T541.

## Setup

Consume T561 as boundary authority. The admitted bounded domain-native class is:

- T559 record-finality transport-square survivor
- T560 handoff/rotation/repair transfer survivor
- T561 third multicover seal/handoff fixture

The frozen source-variable set remains unchanged:

- finite event cover
- local finality sections
- restriction morphisms
- settlement obstruction witness
- transport consistency square
- allowed refinement steps

The frozen mature absorbers and falsifiers remain the T557-T561 set.

## Success Criteria

- T561 selected T562.
- The admitted class remains exactly the T559, T560, and T561 fixtures.
- The complete frozen contract admits every bounded fixture.
- Dropping any frozen source variable produces the expected failure mode across
  every admitted fixture.
- Dropping any core boundary, mature absorber boundary, or falsifier boundary
  produces the expected failure mode across every admitted fixture.
- The next packet is absorber separation, not source-law, TAF4, TAF8,
  claim-ledger, public-posture, or cross-repo movement.

## Failure Criteria

T562 fails if:

- any frozen source variable is renamed, dropped, or silently changed;
- any admitted fixture still admits after a one-at-a-time source-variable drop;
- any core, absorber, or falsifier boundary can be dropped without changing the
  admission status;
- absorber separation is skipped and minimality is promoted to source-law
  status;
- target labels, Lorentzian structure, cross-repo truth, Observerse replay,
  APRD replay, TAF8 execution, or direct TAF4 movement enters the success
  criterion;
- claim rows, Canon Index tiers, canon verdicts, public posture, external
  publication, or cross-repo truth are moved.

## Known Physics Constraints

No physics, geometry, causal-set, Lorentzian, quantum, source-law, or
shadow-protection claim is earned by this gate.

## Implementation Result

Status: implemented.

T562 finds that every frozen source variable, core boundary, mature absorber
boundary, and falsifier boundary is load-bearing across the admitted bounded
class. The complete packet admits T559, T560, and the T561 multicover fixture;
single-condition removals produce the predeclared failure modes.

T562 selects `t563_domain_native_sheaf_transport_absorber_separation_gate` as
the next TAF11 gate. It does not establish a source law, move TAF4, execute
TAF8, or change claim/canon/public posture.

## Run Command

```bash
python -m models.t562_domain_native_sheaf_transport_minimality_gate --write-results
python -m unittest tests.test_t562_domain_native_sheaf_transport_minimality_gate -v
```

## Boundary

T562 is a minimality gate inside the bounded TAF11 sheaf transport route. It
can justify an absorber-separation gate, but it does not supply evidence strong
enough for claim-ledger, Canon Index, source-law, public-posture, TAF4, or TAF8
movement.
