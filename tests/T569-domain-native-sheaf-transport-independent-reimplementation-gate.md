# T569: Domain-Native Sheaf Transport Independent Reimplementation Gate

## Purpose

T569 consumes T568's independent-reimplementation burden and rebuilds the
strengthened TAF11 semantic generator from the declared semantic contract, not
from T567/T568 fixture labels, before any source-law, claim, canon,
public-posture, TAF4, TAF8, S1, external-publication, or cross-repo movement.

## Executable Artifact

- Model: `models/t569_domain_native_sheaf_transport_independent_reimplementation_gate.py`
- Test: `tests/test_t569_domain_native_sheaf_transport_independent_reimplementation_gate.py`
- Results:
  - `results/T569-domain-native-sheaf-transport-independent-reimplementation-gate-v0.1.json`
  - `results/T569-domain-native-sheaf-transport-independent-reimplementation-gate-v0.1-results.md`

## Result

Status: implemented / review-only.

The independent reimplementation reconstructs the T568 semantic generator with
no fixture-label fields. It uses the frozen source variables and absorber
boundaries, plus these semantic requirements:

- nontrivial obstruction witness;
- noncommuting transport square;
- forced finality-native payload;
- target-blind language.

The rebuilt selector matches the T568 contract on a fresh panel. It admits two
fresh finality-native holdouts:

- `escrow_window_rotation_holdout`;
- `checkpoint_quorum_handoff_holdout`.

It rejects four controls:

- `same_neighbor_trivial_gluing_control`;
- `target_geometry_language_import_control`;
- `payload_optional_near_miss_control`;
- `fixture_label_alias_control`.

This closes the fixture-label replay risk for this review step. It does not
earn public source-law status, claim-ledger movement, Canon Index movement,
canon-verdict movement, public-posture movement, TAF4 movement, TAF8 execution,
S1 movement, external publication, or cross-repo truth movement.

## Next Burden

T569 names the next executable TAF11 burden:
`t570_domain_native_sheaf_transport_fresh_family_stress_gate`.

That packet should stress the independently reimplemented generator against a
fresh family or sharply explain why no fresh-family stress is available before
any claim, canon, public-posture, TAF4, TAF8, S1, or source-law movement.
