# advance-secondary — critique log

## Self-critique A
- Distinctive obligation present? The ROM §2 budget tension is the whole point —
  encoded as a mandatory "Budget read" gate (step 2), a `deferred_budget_tension`
  outcome, a budget section in the report, and Failure mode "starving the
  primary". PASS.
- Does it overstep into allocation? No — it may only FLAG tension; setting the
  budget is governance/Phase-4. "Must not" + Failure mode "allocation overreach".
  PASS.
- Authority leak (status canonization)? No — patch-first, conservative bar,
  accept-gated; core invariant guards it. PASS.
- Primacy reassignment? Routed via challenge-worthiness + line-review +
  lifecycle-review; never decided here. PASS.
- Cross-cluster discipline (ROM §4/§8): secondary defined by distant-cluster
  breadth or specialist survival arg; Failure mode "cluster-headcount illusion".
  PASS.
- Failure preservation: negative-result outcome + info-gain. PASS.

## Revision after A
- Added explicit "more conservative than advance-primary" framing to scoring
  authority and step 7, so the two sibling workflows are differentiated beyond
  the line they target.
- Added challenge-worthiness signal path to challenge-primary so a maturing
  secondary has a defined route to contest the primary.

## Self-critique B
- Section parity with locked exemplars: full set present incl. Consumes/Emits,
  Core invariant, Outcome classes, Docket shape, cheap test. PASS.
- Route targets exist or docketed: line-review, lifecycle-review,
  challenge-primary (this family), integrate-results, information-portfolio,
  docket. PASS.
- Differentiation from advance-primary is real (budget gate, conservative bar,
  explorations/ as a write surface, primacy-contention step) — not a clone. PASS.
- LOCK-CANDIDATE: open questions (budget criterion, claim-bar gap, acceptance
  owner, incubated-eligibility) are non-gating and docketed. → v1.0 LOCK-CANDIDATE.

## Revision after B
- None structural; confirmed budget read runs BEFORE move execution in both the
  procedure and the decomposition notes.
