# Critique log — explore/line-incubation

Process: draft → self-critique A → revise → self-critique B → revise → finalize
(autonomous run, DEC-019).

## Self-critique A (structure / contract conformance)
- Frontmatter: 8 document-contract fields present (document_type: workflow;
  primary_reader: automation; read_pattern: current_state; write_pattern:
  patch_proposal; authority: canonical_workflow; output_authority: <fit>;
  summarizable: false; unit_of_review: <fit>). PASS.
- Section set matches the locked exemplars (line-review / lifecycle-review): title;
  Family/Mode/Status; Purpose; Authority boundaries; Read surfaces; Write surfaces;
  Memory interface; Registry interactions; Procedure; Outputs; Escalation triggers;
  Failure modes; Success criteria (w/ cheap test); Future automation decomposition
  notes; Verdict block; Open questions. PASS.
- Mode stated explicitly (search workflows say so; "both" workflows name the mode
  per phase). PASS.

## Self-critique B (operating-model / authority conformance)
- Authority boundaries: workflow PROPOSES only; never creates/moves canonical
  state; non-owned actions route OUT to the owning workflow. Checked against §11
  authority order and the explore-family boundary (expand/search; creation,
  lifecycle, standing belong to govern).
- "research line" terminology; two-axis lifecycle (stage vs status) respected
  (only proposes stage candidates; DEC-018).
- Memory INTERFACE only (reads load surface / writes learning-return); does not
  depend on memory existing. No automation/memory implementation.
- Every route targets an existing workflow OR is docketed as
  undefined-workflow-needed (line-intake / portfolio-review / docket-triage not yet
  locked are docketed, candidate preserved).
- No invented policy: missing decisions are Open Questions + governance-docket
  items, not unilateral rules.
- Verdict block matches operating-model §10 common currency.

## Revisions applied
- Tightened the proposes-vs-acts boundary and added explicit
  why_source_workflow_cannot_decide on routed candidate objects.
- Added cheap-test sentence to Success criteria; added Future-automation
  deterministic-vs-judgment split and Phase-4 coverage questions.
- Confirmed synthesis vs reassessment kept constitutionally distinct (where relevant).

## Lock decision
v1.0 LOCK-CANDIDATE — no lock-gating blocker. All open items are docketed and
non-gating (DEC-019 lock criteria).
