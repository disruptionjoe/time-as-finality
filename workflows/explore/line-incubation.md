# Line Incubation

**Family:** explore
**Mode:** both
**Status:** placeholder (Phase 2). Behavior designed in Phase 3.

## Purpose
Move seeded/explored lines up the lifecycle toward `validated` through light,
bounded validation — generating in search mode, then checking in evaluation mode.

## Expected inputs
Early-lifecycle entries in `registries/line-registry.md` and their sketches;
`protocols/scoring-protocol.md` (Phase 3).

## Expected outputs
Lifecycle-state updates; validation notes; information-gain entries; verdict block.

## Related registries
`line-registry` (read+write), `information-portfolio` (write).

## Open questions
What are the `validated` criteria? When does a line hand off to exploit?
