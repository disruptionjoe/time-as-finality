---
document_type: synthesis_preflight
primary_reader: governance
read_pattern: current_state
write_pattern: append
authority: non_authoritative
summarizable: true
source_queue_item: third_batch_4
owner_line: RL-005
claim_status_change: none
---

# H7 Physical-Deletion Substrate Preflight

## Status

Non-authoritative preflight artifact for third-batch task 4. This file does
not edit `CLAIM-LEDGER.md`, `ROADMAP.md`, code, tests, results, or
open-problem files. It is not H7 evidence, not physical-arrow evidence, and
not a thermodynamic derivation.

## Read Surfaces

- `open-problems/h7-physical-deletion-substrate-handoff.md`.
- `CLAIM-LEDGER.md`: H7 canon row and the 2026-06-21 H7 physical-deletion
  substrate handoff entry.

## Preflight Verdict

Current classification:

```text
H7 remains weakened_conditional.
```

The only admissible next move is a named substrate packet that can be screened
before any H7 reinstatement language appears. The packet must show that the
reverse is `physical_record_deletion`, not access loss, support-copy removal,
provenance loss, practical erasure hardness, or observer-boundary restriction.

## Frozen Object Required Before Execution

Any future executable artifact must freeze these fields before evaluation:

| Field | Required content |
| --- | --- |
| `substrate_id` | Named finite physical substrate, not a family slogan. |
| `record_token` | Encoding of one record token in substrate-native terms. |
| `write_operation` | Operation that creates or stabilizes the token. |
| `delete_operation` | Operation claimed to erase the token. |
| `reverse_edge_class` | Must be exactly `physical_record_deletion`. |
| `future_operation_split` | Task-natural future operation allegedly changed by deletion topology. |
| `absorber_vector` | Free energy, blank capacity, sink/export, boundary, provenance, source-copy, reversible-control fields. |
| `substrate_absorbers` | Metastability, code, bath, pump, reservoir, sector, gauge, or horizon data as applicable. |
| `control_class` | Allowed controls used to distinguish impossible from merely unavailable. |
| `null_controls` | Hidden sink/export, hidden copy, denied control, idealization, and finite-rate controls. |

## Acceptance Criteria

- The substrate and deletion task are named before any H7-relevance judgment.
- The reverse edge is `physical_record_deletion` under the source note's
  taxonomy.
- The absorber vector is matched across the comparison pair.
- Source-copy handles, reversible controls, and sink/export histories are
  explicitly accounted for.
- The future-operation split persists after absorber matching.
- The impossibility claim is finite and substrate-native, not an infinite
  barrier, exact code, horizon-only inaccessibility, or sector-rule ideal.
- The output verdict is one of:

```text
admit_for_h7_audit
reject_as_null
insufficient_packet
```

`admit_for_h7_audit` means only that a later audit is worth running. It is not
promotion.

## Null Or Demotion Conditions

Treat the candidate as null for H7 if any condition holds:

- No named substrate is supplied.
- The proposal shows long lifetime, metastability, or high finite barrier only.
- The reverse is access revocation, support-copy removal, provenance loss, or
  observer-boundary change.
- The sink, reservoir, work budget, exported history, or blank capacity changes
  across the pair.
- Source-copy correlations or reversible controls restore the reverse.
- The claim depends on denied controls rather than constructor-impossibility.
- The future-operation split disappears once absorber fields are matched.
- The argument uses infinite barriers, exact superselection, perfect codes, or
  another ideal limit without a finite operational reading.
- The allowed control class is unstated.

If the candidate fails after a frozen packet exists, record the absorber that
killed it and keep H7 demoted.

## No-Promotion Guardrails

- Do not change H7 out of `weakened_conditional`.
- Do not call practical irreversibility, high erasure cost, or protected memory
  physical-arrow evidence.
- Do not use this preflight to write thermodynamic-arrow prose.
- Do not cite a candidate as H7 support unless it first survives the later
  T145/T148/T152/T160/T168-style audit under matched accounting.
- Do not treat capability/topology residue as a physical arrow.
- Do not edit ledger, roadmap, tests, models, results, or open problems from
  this preflight.

## Next Executable Artifact Shape

The next artifact should be a single-substrate audit packet with this shape:

```text
artifact_type: h7_physical_deletion_substrate_packet
substrate_id:
record_token:
write_operation:
delete_operation:
reverse_edge_class: physical_record_deletion
future_operation_target:
absorber_vector:
  free_energy:
  blank_capacity:
  sink_or_export_history:
  observer_or_control_boundary:
  provenance_or_authority:
  source_copy_status:
  reversible_control_access:
substrate_specific_absorbers:
allowed_control_class:
hidden_resource_controls:
finite_operational_reading:
verdict: admit_for_h7_audit | reject_as_null | insufficient_packet
blocking_absorber_if_null:
claim_impact: no_status_change
```

No multi-substrate survey should be run before one packet freezes these fields.
