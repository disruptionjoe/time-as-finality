# Bridge-Functor Admission Packet Template

## Status

Packet status:

```text
draft | admitted_review_target | rejected | demoted
```

Claim posture:

```text
no_claim_movement
```

This packet does not promote claims, change public posture, alter the North
Star, or assert new physics.

## Candidate

Candidate name:

```text
...
```

Closed lineage being reopened or explained:

```text
...
```

Why this is not a minor restart:

```text
...
```

## Source Structure

```text
Y   =
A   =
O   =
pi  =
T   =
h   =
B   =
Cap =
K   =
R_K =
```

## Domain-Native Law

Declare the law, theorem, cost, measure, morphism, protocol, or source-backed
result before selecting witness pairs.

```text
L =
source/status =
```

Failure if:

```text
L is posthoc, pair-specific, imported by analogy, or not native to the domain.
```

## Absorber Stack

List absorbers in order. Grant each absorber in its strongest normal native
form before assigning residue.

| order | absorber | granted form | expected effect | failure if undeclared |
|---:|---|---|---|---|
| 1 |  |  |  |  |
| 2 |  |  |  |  |
| 3 |  |  |  |  |

Common absorbers to consider:

- full state;
- full history;
- provenance/log/ledger/server completion;
- full competency/task-success profile;
- resource preorder or monotone vector;
- transition-system completion;
- boundary enlargement or permission completion;
- law-sector completion;
- gauge, coordinate, basis, or relabeling quotient;
- statistical uncertainty or tolerance envelope;
- sufficient statistic, belief state, or process equivalence.

## Witness And Controls

Visible fiber or witness class:

```text
...
```

Capability spread under `R_K`:

```text
...
```

Positive controls:

```text
...
```

Negative controls:

```text
...
```

Minimal enrichment:

```text
...
```

Why the enrichment is smaller than `(pi, Cap)`:

```text
...
```

## Composite Explanation Check

What phenomenon did we think we did not understand?

```text
...
```

How does the absorber stack explain it?

```text
...
```

Does this explanation remove the need for new TaF structure?

```text
yes | no | unresolved
```

## Composite Residual Check

After granting the full stack, what remains?

```text
...
```

Can the residual be removed by any declared absorber?

```text
yes | no | unresolved
```

If no, state the review target:

```text
...
```

## Outcome

Choose exactly one:

```text
SINGLE_ABSORBER_DEMOTION
COMPOSITE_ABSORBER_EXPLANATION
COMPOSITE_RESIDUAL_REVIEW_TARGET
PACKET_REJECTED_UNDECLARED_ABSORBERS
PACKET_REJECTED_POSTHOC_LAW
PACKET_REJECTED_TRIVIAL_DASHBOARD
PACKET_REJECTED_PROMOTION_SHORTCUT
```

## Demotion Path

This packet demotes if:

```text
...
```

Allowed demotion labels:

```text
bookkeeping
absorber_calibration
composite_explanation_only
analogy_only
closed_route_minor_restart
```

## Governance Checks

```text
claim-ledger movement: no
roadmap movement: no
README movement: no
North Star movement: no
public-posture movement: no
hard-policy movement: no
protected-license movement: no
external-publication movement: no
cross-repo truth movement: no
```
