---
document_type: template
primary_reader: agents
read_pattern: copy_for_new_artifact
write_pattern: do_not_edit_in_place
authority: advisory
summarizable: false
---

# North Star Shadow Audit Template

Use this template before turning any North Star vision sentence into a research
artifact. The goal is to test whether an observer shadow preserves a typed
capability object under native comparison, while granting mature absorber
fields their legitimate state and theorems.

## Claim Under Test

Vision sentence:

Research route:

Expected artifact type:

Not claimed:

## Known Source Domain

Known theory or mature field:

Legitimate absorber state:

Native operations:

Native equivalences:

Native comparison or preorder:

## Source And Shadow

Source structure `Y`:

Observed base or interface `X`:

Observer/access profile `O`:

Projection or shadow map `pi_O : Y -> X`:

Visible equivalence `~=_X`:

Admissible source states `A subset Y`:

Access boundary, horizon, budget, or resource condition:

## Capability Object

Task family `T`:

Horizon `h`:

Capability object `Cap_{O,T,h,B}`:

Capability codomain `K`:

Native capability comparison `R_K`:

Why `Cap` is domain-native or operationally forced:

Positive preservation control:

Negative non-factorization fixture:

Capability spread over visible fibers:

```text
Spread_Cap([x]) =
```

## Absorber Passes

Native state completion tested:

Native theorem absorption tested:

Repair, tolerance, or threshold absorption tested:

Gauge, coordinate, basis, phase, relabeling, and representation absorption
tested:

Trivial enrichment tested:

```text
pi'(y) = (pi_O(y), Cap(y))
```

## Geometry Discipline

Geometric words used:

For each word, mark one:

```text
earned | example-only | source-vocabulary | quarantined | delete
```

Finite-to-smooth bridge, if any:

Declared functor, discretization, limit, or comparison map:

## Verdict

Projection-sufficient?

Projection-insufficient?

Minimal enrichment or repair:

Residue label:

```text
canonical residue | formal audit residue | typed toy-model residue |
translation residue | heuristic residue | redundant or demoted
```

Kill condition:

Next bounded artifact:
